# Plan adversarial reviews (4 lenses)

refuted research claims: 11/12

## Lens: completeness-vs-brief + testability + safety + migration-fidelity — verdict: request-changes

- **[blocker]** 00-index.md :: How to read this plan / File map
  - Concern: Only 4 of 13 plan files were produced. The remaining 9 files (04 through 12) are not yet written. The plan claims to cover §5.1-§5.6 in full but acceptance criteria for §5.2 (Script #1 patch), §5.3 (Script #2 profiles), §5.4 (migrated skill), §5.5 (migration note), §6.B/§6.C/§6.D/§6.E, §8 (test strategy + toolchain), §10 (sub-agent delegation), and §12 (risks/open questions) cannot be reviewed or executed because the corresponding plan files do not exist. Execution cannot start on §5.2, §5.3, §5.4, or §5.5 work without these files.
  - Fix: Persist the 9 missing files (04-script-1-patch.md, 05-script-1-task-e-toggle.md, 06-script-2-profiles.md, 07-skill-creator-migration.md, 08-migration-note-format.md, 09-test-strategy.md, 10-toolchain-and-conventions.md, 11-sub-agent-delegation-map.md, 12-risks-and-open-questions.md) before any Phase 5 delegation begins.

- **[blocker]** 01-overview.md :: Acceptance criteria 2 (Script #1 §5.2)
  - Concern: Plan commits to shipping Script #1 but acceptance criterion 2.4 says line drift "is retried with --force" — this is a contradiction. If the validation gate is all-or-nothing (2.3), then a LINE_DRIFT must abort, not auto-retry with --force. The plan is internally inconsistent about whether --force is a user-invoked flag or an automatic fallback. As written the script will silently bypass the all-or-nothing guarantee.
  - Fix: Rewrite 2.4 to state: "Line drift → exit 2 with LINE_DRIFT diagnostic; operator must re-run with --force to retry line-only. Default mode never auto-bypasses the text+line match."

- **[blocker]** 01-overview.md :: Acceptance criteria 4 (Migrated skill-creator §5.4)
  - Concern: Acceptance criteria 4.2 declares description <= 1024 chars assuming the patched cap. But the brief explicitly lists as an open question whether the cap will be raised at all, and §6.E is opt-in. The plan therefore commits to a description length that may be illegal in the unpatched Hermes install. The plan should ship a hard guard: the installer must verify either (a) the cap is raised, or (b) the SKILL.md description is <= 60 chars. Without this, the migrated skill will be silently truncated at install time.
  - Fix: Add AC 4.10: "Installer MUST detect the active cap (60 vs 1024) and reject the install with a clear bilingual error if the description exceeds the active cap, or refuse to install and instruct the operator to apply Script #1 first."

- **[major]** 03-plugin-spec.md :: Cap-raise mechanism (monkey-patch)
  - Concern: The "monkey-patch in-process" approach for the cap raise is unsound. The cap constant `60` is checked at multiple sites (not just in `extract_skill_description`); the plan only rebinds the function and the call site in `prompt_builder.py:1090`. Other sites that read the constant (or the function via a captured local import) will still see 60. The plan's claim that "the actual cap raise is performed by Script #1 against a user-owned Hermes checkout" makes the runtime monkey-patch a redundant and confusing second mechanism.
  - Fix: Decide on one mechanism: either (a) the runtime monkey-patch (in which case the plan must enumerate every call site, not just prompt_builder.py:1090, and prove the rebind reaches all of them — including the `tools/skills_tool.py:95` MAX_DESCRIPTION_LENGTH reference), or (b) Script #1 is the sole mechanism and the plugin only registers the skill, never patches. The current "both" path is untestable and contradictory.

- **[major]** 01-overview.md :: Acceptance criteria 3 (Script #2 §5.3)
  - Concern: AC 3.1 says "audits the hermes (default) profile AND every named profile returned by hermes_cli.profiles.list_profiles()". The plan provides no fallback or test fixture for what `list_profiles()` returns when the operator has no profiles configured (empty list, raises, returns sentinel). Without a fixture spec, 100% coverage is not achievable. Similarly, AC 3.4 references `set_hermes_home_override` but the plan never confirms this function's signature or return type — open question Q4 admits this is unresolved.
  - Fix: Add an explicit test fixture strategy in the missing 06-script-2-profiles.md: (a) empty-profile case, (b) single-profile case, (c) N-profile case, (d) missing ~/.hermes case. Add a stub for `set_hermes_home_override` and `list_profiles()` with documented return shapes, or mark the test strategy as pending Q4 resolution.

- **[major]** 02-architecture.md :: Data flow / Failure modes
  - Concern: The plan's "Graceful degradation" section says "if the patch is NOT applied, the 60-char cap remains; the migrated skill's description MUST be <= 60 chars in that mode" — but the acceptance criteria 4.2 commits to description <= 1024. These contradict. The plan also says the plugin emits a "one-time bilingual advisory at session start" when unpatched, but provides no mechanism to prevent that advisory from firing on every single session (it claims "one-time" without defining the persistence). No file or registry is named.
  - Fix: Either (a) drop the "one-time" claim and emit the advisory every session, or (b) name the persistence file (e.g. ~/.hermes/plugins/hermes-skill-creator-plugin/.advisory_shown) and add a test for it. Resolve the 60 vs 1024 description-length contradiction by making the active cap authoritative.

- **[major]** 03-plugin-spec.md :: Cap-raise mechanism (declarative, feature-flagged)
  - Concern: The vendored `extract_skill_description_patched.py` imports `from agent.skill_utils import extract_skill_description as _orig` — this import is never used (the patched function never calls _orig). This will trigger a ruff F401 (unused import) and is dead code. Also, the patch is described as "applied at install time" in the goal but then "applied as a Python-level monkey-patch" in the hard rule — another internal contradiction.
  - Fix: Remove the unused `_orig` import. Resolve the "applied at install time" vs "applied as monkey-patch" contradiction by picking one and updating both the goal and the hard rule.

- **[major]** 02-architecture.md :: Sequence / Plugin install path
  - Concern: The plan introduces `python -m hermes_skill_creator_plugin.install` as a thin installer, but never specifies what "copies the plugin into ~/.hermes/plugins/" actually entails. Hermes's plugin discovery mechanism (where it looks for plugins, manifest validation, install lifecycle) is referenced but not pinned. Without a fixture or a confirmed reading of the Hermes harness, 100% coverage of the installer is not achievable. The plan's TDD section (3.4-3.6) requires a "fixture HERMES_HOME" but no such fixture strategy is defined anywhere in the four files.
  - Fix: Add a `tests/fixtures/hermes_home/` directory spec to the missing 09-test-strategy.md. Specify the minimum file tree the fixture must contain, and a `conftest.py` fixture that injects it via `monkeypatch.setenv('HERMES_HOME', tmp_path)`. Without this, integration test claims are unprovable.

- **[major]** 00-index.md :: Hard constraints
  - Concern: The hard-constraint list says "The installed Hermes at ~/.hermes/hermes-agent is never modified" — but the plan describes a runtime monkey-patch that rebinds `agent.skill_utils.extract_skill_description` in-process. The agent module lives in `~/.hermes/hermes-agent` and the rebind mutates the module object's attribute table. Strictly speaking, this is a Python-level mutation of a module owned by the read-only target. The constraint needs to be tightened: "No filesystem write to ~/.hermes/hermes-agent, and no in-process mutation of its modules either, except via the controlled installer that copies (not edits) the vendored function."
  - Fix: Tighten the safety constraint to forbid both filesystem writes and in-process module mutation of ~/.hermes/hermes-agent. Move all cap-raise to Script #1's source-edit path against a user-owned checkout; the plugin must not patch at all.

- **[major]** 01-overview.md :: Acceptance criteria 4 (Migrated skill-creator §5.4)
  - Concern: AC 4.5 says "Every Claude-specific invocation replaced per the T3 inventory" but the T3 inventory itself is not in the four plan files. Without the inventory list, the plan cannot be reviewed for migration fidelity. The plan references "T5" (related_skills in-repo only) and "T3" without enumerating them.
  - Fix: Either inline the T3 inventory in 07-skill-creator-migration.md (which is one of the missing files) or, if those files are still pending, defer acceptance for AC 4.5 until 07 is written.

- **[major]** 01-overview.md :: Acceptance criteria 2 (Script #1 §5.2)
  - Concern: AC 2.8 commits to "opt-in Task E built-in-prompt toggle: --task-e-redirect applies sites 1-7" but the four files do not enumerate sites 1-7, do not specify the per-site text+line anchors, and do not specify what the redirects themselves do. The plan defers this entirely to the missing 05-script-1-task-e-toggle.md. Without the per-site anchor list, the multi-signal targeting AC (2.2) and the all-or-nothing gate (2.3) cannot be verified to cover all 7 sites.
  - Fix: When persisting 05, include a per-site table: site_id, file:line, current_text (8+ char anchor), replacement_text, post-replacement line shift. This is the minimum to make AC 2.2 and 2.3 reviewable.

- **[major]** 03-plugin-spec.md :: Skill registration / i18n messages
  - Concern: The plan imports `ADVISORY_SKILL_PRESENT_EN` and `ADVISORY_SKILL_PRESENT_HU` from i18n modules that are listed in the layout (`i18n/messages_en.py`, `i18n/messages_hu.py`) but no test case verifies the bilingual format. The plan's hard rule is bilingual console/log messages; without a test asserting that the registration log contains both EN and HU substrings, the bilingual rule is unenforced.
  - Fix: Add test cases `test_register_log_is_bilingual` and `test_advisory_messages_contain_en_and_hu` in `tests/unit/test_skill_register.py`. Define the canonical bilingual format in a docstring or constant.

- **[major]** 02-architecture.md :: Data flow / Patch path
  - Concern: AC 2.4 and the "atomic write" description are stated but the atomic write protocol is not defined. The plan says "write to .patch.tmp + rename" but does not address (a) cross-filesystem rename failure, (b) the case where the original file is opened by another process (Hermes itself), or (c) file permission / ownership preservation. No test case covers atomic write failure modes.
  - Fix: Specify the atomic write protocol in 04-script-1-patch.md: same-dir tmpfile, os.replace(), restore on failure, preserve mode bits via stat().st_mode, test with `monkeypatch.setattr(os, 'replace', raises)` and `monkeypatch.setattr(os, 'chmod', ...)`.

- **[major]** 03-plugin-spec.md :: 100% code + logic coverage targets
  - Concern: The plan claims 100% coverage is achievable via the listed test cases, but `register` is described as catching a KeyError if frontmatter is missing, while the test list only covers the happy path. The plan acknowledges "frontmatter missing → KeyError is caught and logged" as a branch needing coverage but does not include the test case in the test list. This is an off-by-one coverage gap.
  - Fix: Add test cases: `test_register_missing_frontmatter_raises_and_logs`, `test_register_empty_frontmatter_raises_and_logs`, `test_register_invalid_yaml_logs_and_skips`.

- **[minor]** 01-overview.md :: Acceptance criteria 2.10
  - Concern: Exit codes are listed as 0, 1, 2, 3, 4, 5. But there is no mapping from exit code 2 ("line drift") to a documented remediation. The plan says LINE_DRIFT diagnostic is emitted but does not commit to printing the remediation in the diagnostic itself.
  - Fix: Add a remediation hint to the LINE_DRIFT diagnostic, e.g. "Re-run with --force to retry line-only." Include a test that asserts the diagnostic contains the hint.

- **[minor]** 03-plugin-spec.md :: plugin.json manifest
  - Concern: The manifest declares `kind=skill_authoring` but the plan does not verify this is a valid `kind` value in the Hermes v0.16.0 plugin spec. The plan defers to a future subprocess-based validator test, but does not pin the validator's expected output shape.
  - Fix: Add a fixture under `tests/fixtures/valid_manifests/` containing a known-good plugin.json from Hermes v0.16.0, and assert the new manifest matches the schema's accepted `kind` values.

- **[minor]** 02-architecture.md :: Component diagram / Hermes v0.16.0 paths
  - Concern: The diagram cites specific line numbers (`:647-655`, `:1070-1090`, `:95`, `:378`) for v0.16.0 paths. The plan's open questions list (Q9) explicitly admits these are not verified. A line drift in any of these would invalidate the plan's anchor list. The plan should commit to re-reading at planning time, but the workflow does not enforce that.
  - Fix: Add a `validate_anchors.sh` (or a Python equivalent under tests/integration/) that re-reads the cited files and asserts the cited line ranges still contain the expected text. Run as part of the test suite, not as a one-time planning check.

- **[minor]** 01-overview.md :: Acceptance criteria 5 (Migration note §5.5)
  - Concern: AC 5.1 places `MIGRATION.md` at the worktree root, but AC 2 also implies Script #1 emits the migration note via `--emit-migration-note`. The plan does not specify whether the migration note lives in the worktree (source-controlled) or in the target Hermes checkout (regenerated per-install). The two locations have very different audit and upstream-resync implications.
  - Fix: Pin the location: either (a) MIGRATION.md is committed in the worktree as the source of truth, or (b) MIGRATION.md is regenerated into the target checkout on every `--emit-migration-note` and is never source-controlled. The plan must commit to one.

- **[minor]** 00-index.md :: Hard constraints / Worktree+PR
  - Concern: The constraint says "commits after every task; PR when the worktree spans more than the current task" but the plan does not specify the commit granularity. With 9 missing files and ~30 acceptance criteria, a "commit per task" rule is ambiguous. Without a clear commit boundary spec, the worktree will accumulate dozens of uncommitted changes and the PR will be unreviewable.
  - Fix: Define commit boundaries in 10-toolchain-and-conventions.md (also missing): one commit per acceptance criterion cluster (1.x, 2.x, ..., 6.x), or one commit per phase (plugin, script 1, script 2, skill, note). The 10 file must commit to a granularity before Phase 1 starts.

- **[minor]** 02-architecture.md :: Data flow / Script #2 --apply path
  - Concern: AC 3.8 says "calls clear_skills_system_prompt_cache(clear_snapshot=True)" but does not pin whether this is a Hermes public API, an internal helper, or a function Script #2 must add. The plan defers to dispute D9 in open questions, but the AC is committed. If the function does not exist, the AC is unfulfillable.
  - Fix: Either (a) confirm `clear_skills_system_prompt_cache` exists in v0.16.0 (read agent/skill_utils.py or similar), or (b) rewrite the AC to call a documented public API (e.g. delete `~/.skills_prompt_snapshot.json` directly, or call the documented cache-invalidation method).

- **[minor]** 01-overview.md :: Acceptance criteria 6 (Plan files §5.6)
  - Concern: AC 6.1 says "All files <= 500 lines" but the four produced files include 02-architecture.md at 147 lines and 03-plugin-spec.md at 147 lines — well under. However, the open questions list in the summary references 10 questions, and the missing 12-risks-and-open-questions.md must enumerate them. If that file exceeds 500 lines, AC 6.1 is violated. The plan should commit to a per-section line budget, not just a per-file cap.
  - Fix: Allocate a line budget per file in 00-index.md: 00 (80), 01 (120), 02 (200), 03 (200), 04 (400), 05 (250), 06 (400), 07 (450), 08 (150), 09 (350), 10 (250), 11 (200), 12 (200). Sum < 3000. If any file exceeds its budget, the plan is rejected.

- **[minor]** 02-architecture.md :: Data flow / Migrated skill runtime path
  - Concern: The runtime path says the agent calls "skill_view(name='skill-creator')" — but the plan never confirms `skill_view` is the correct Hermes tool name. The plan's tool-name convention (AC 4.7) says "tool_name.lower() in (...)" but the canonical tool list (skills_list, skill_view, skill_manage) is not pinned. Q3 in the open questions confirms the streaming JSON event shape for eval viewer is also TBD. The runtime sequence is therefore partly speculative.
  - Fix: Add a test that asserts the SKILL.md body's instructions reference the documented Hermes tool names from the v0.16.0 source, not assumed names. This is a static-text test against the SKILL.md body.

- **[nit]** 01-overview.md :: Acceptance criteria 2.1
  - Concern: AC 2.1 says idempotent "exits 0 with `OK: already patched`" but the AC 1.1 uses "OK: already installed" — the wording is inconsistent. The bilingual rule means the string should appear in both EN and HU, but the AC just shows one half.
  - Fix: Standardize the OK format: `OK: already patched / OK: már javítva` and `OK: already installed / OK: már telepítve`. Add bilingual assertions to the test cases.

- **[nit]** 03-plugin-spec.md :: 100% code + logic coverage targets
  - Concern: Test `test_zwj_emoji_no_split_invariant` documents the behavior as "verifies the slice is on code points, not graphemes; documented behaviour, not a bug" — this is fine as a test, but the test's purpose is unclear. The acceptance criteria should clarify whether the patched function is required to be grapheme-safe or code-point-safe. The "documented behaviour, not a bug" hedge suggests the test is testing a known weak spot.
  - Fix: Either drop the test (the behavior is uninteresting) or move it to a `test_known_weakness` module with a TODO marker to revisit when grapheme support lands. Do not let it count toward the 100% coverage of the *intended* behavior.

- **[nit]** 00-index.md :: Bilingual legend
  - Concern: The bilingual legend gives an example `print("[en] ... / [hu] ...")` but does not commit to a single canonical format. With 5+ scripts and 30+ log lines, an inconsistent format will be hard to grep. The plan should commit to a format and a test.
  - Fix: Add to the bilingual rule: "All console/log lines MUST match the regex `\\[en\\] .+ / \\[hu\\] .+`." Add a test that asserts every console/log line in the project matches this regex.

---

## Lens: testability-and-coverage — verdict: request-changes

- **[blocker]** 01-overview.md :: Deliverables table — §5.2 Script #1 acceptance criteria
  - Concern: Test cases for the all-or-nothing validation gate (AC 2.3), the LINE_DRIFT diagnostic (AC 2.4), and the exit-code matrix (AC 2.10) are listed in 01-overview.md as acceptance criteria but NO plan file contains the test list for Script #1 itself. Plan files 04-script-1-patch.md and 05-script-1-task-e-toggle.md are referenced but were never persisted ("The StructuredOutput tool returned a validation error and aborted the batch"). Without these, no TDD scaffolding exists for ~70% of the deliverables and the 100% coverage claim is unenforceable. Execution cannot start for §5.2 / §5.3 / §5.4 / §5.5 / §6.B / §6.C / §6.D / §6.E until the remaining 9 plan files are written.
  - Fix: Re-run the batched StructuredOutput with the full 13 files. Until then, gate Phase 5 (Tasks B/C/D) on the missing plan files landing and being reviewed.

- **[major]** 03-plugin-spec.md :: Cap-raise mechanism — patch_runtime.py
  - Concern: The patch_runtime.py monkey-patch rebinds `skill_utils.extract_skill_description` AND `prompt_builder.extract_skill_description` in-process. The vendored `_patched` function in `resources/extract_skill_description_patched.py` imports `extract_skill_description as _orig` from `agent.skill_utils` but never calls it (the listed body replaces it wholesale). Test `test_rebind_prompt_builder` (AC 1.2 / 03 §TDD) assumes the call site at `agent/prompt_builder.py:1090` is reachable via `from agent import prompt_builder; prompt_builder.extract_skill_description`, but the plan does not verify that `prompt_builder` imported `extract_skill_description` as a module-level name (it may be `from agent.skill_utils import extract_skill_description` — these are equivalent under `import` semantics, but if the call site uses `agent.skill_utils.extract_skill_description(...)` directly, the monkey-patch on the `skill_utils` module attribute is sufficient and the `prompt_builder` rebind is dead code). The plan needs an explicit test that asserts the rebind path matches the real Hermes source's import style; otherwise 100% line coverage can pass while the cap remains un-raised in production.
  - Fix: Add a test that greps the real `agent/prompt_builder.py` source for the import style and asserts the rebind branch in `maybe_apply_cap_raise` is the correct one. Fixture-based (copies the file into a temp dir). Document both import styles and the matching rebind branch.

- **[major]** 03-plugin-spec.md :: extract_skill_description_patched — test_desc_zwj_emoji_no_split_invariant
  - Concern: The plan documents that slicing on code points (not graphemes) is "documented behaviour, not a bug", but the test case `test_desc_zwj_emoji_no_split_invariant` is described as a ZWJ sequence of len 5 staying intact when `len(desc) == 1024`. This is incoherent: if `len(desc) == 1024` (the cap), no slicing happens and the assertion is trivially true. The actual invariant the test should cover is `len(desc) == 1025` with a ZWJ-emoji straddling the cut point (e.g. position 1023-1027), verifying the slice lands inside a code point and not inside a ZWJ join. As written the test passes vacuously and the branch is un-tested.
  - Fix: Rewrite the test: build a description of length 1025 where a ZWJ-emoji begins at index 1022; assert the truncated result is `desc[:1021] + "..."` (or, if re-engineered to be grapheme-safe, the proper truncation) and assert the slice boundary is NOT inside a code point.

- **[major]** 01-overview.md :: AC 2 — Script #1 acceptance criteria
  - Concern: AC 2.3 (all-or-nothing gate) and AC 2.4 (LINE_DRIFT) imply Script #1 must (a) run a full pre-validation pass across all sites, (b) only then start writing, and (c) write a `.patch.rejected` report on any failure. The plan does not enumerate a test case for the partial-write recovery: e.g. what happens if site 3 of 7 succeeds validation but site 4 fails, and an operator re-runs after fixing the upstream file? The plan must specify a test that simulates a multi-site partial failure and asserts (i) zero bytes are written to the target file, (ii) the .rejected report names the failing site, (iii) the next --check run after the operator fixes the upstream file exits 0. Without this, 100% branch coverage of the validation gate is unprovable.
  - Fix: Add a test case to 04-script-1-patch.md TDD list: `test_partial_failure_zero_writes` (write a fixture where sites 1-3 pass and site 4 is pre-corrupted, assert target file is byte-identical to pre-run state and a .rejected report exists naming site 4).

- **[major]** 01-overview.md :: AC 3 — Script #2 acceptance criteria
  - Concern: AC 3.4, 3.6 require Script #2 to wrap per-profile `do_install` in `set_hermes_home_override` AND mirror `os.environ['HERMES_HOME']`. This is correct per the disputed D7 — but the plan does not specify a test that asserts BOTH operations are performed. A test that only checks `set_hermes_home_override` was entered would pass even if the env-var mirror is forgotten (or vice versa). The plan must enumerate distinct tests for (i) `set_hermes_home_override` enter/exit, (ii) `os.environ['HERMES_HOME']` set/restore, (iii) the env-var mirror survives a `hub_install_or_update` call that internally reads `os.environ['HERMES_HOME']` (D7 dispute). 100% coverage of this dual-path is impossible without three separate tests.
  - Fix: Add three test cases to 06-script-2-profiles.md TDD list: `test_set_hermes_home_override_called`, `test_env_var_mirrored_into_os_environ`, `test_hub_install_reads_mirrored_env` (uses a fake hub that asserts `os.environ['HERMES_HOME']` matches the override).

- **[major]** 01-overview.md :: AC 3.8 — clear_skills_system_prompt_cache
  - Concern: AC 3.8 requires `clear_skills_system_prompt_cache(clear_snapshot=True)` to be called after each successful flip. The plan does not enumerate a test for the failure path: if `clear_skills_system_prompt_cache` raises (e.g. cache file locked), the flip is already persisted, so Script #2 must log the cache-clear failure, leave the persisted state alone, but still exit non-zero. Without an explicit test for the cache-clear-throws branch, 100% logic coverage cannot be demonstrated.
  - Fix: Add a test that monkey-patches `clear_skills_system_prompt_cache` to raise; assert (a) the flip persists, (b) the script exits with a dedicated exit code, (c) the bilingual message names the cache-clear failure.

- **[major]** 03-plugin-spec.md :: TDD test cases — test_register_loads_frontmatter_description
  - Concern: `register()` parses the frontmatter with `yaml.safe_load((skill_dir / "SKILL.md").read_text().split("---")[1])`. The `split("---")[1]` slice is fragile: if the SKILL.md has no closing `---` or has YAML frontmatter containing `---` separators (unusual but possible in `description: "---"`), the slice returns the wrong block. The plan lists `test_register_loads_frontmatter_description` but does not enumerate the negative cases: (i) frontmatter missing entirely, (ii) `---` appearing inside a quoted string, (iii) BOM at file start. The `register()` function's `KeyError` catch in the coverage-targets paragraph is mentioned but no test exercises it.
  - Fix: Replace the naive split with a proper YAML-frontmatter parser (e.g. python-frontmatter) and add tests: missing frontmatter, embedded `---` in description, BOM, multi-doc file. Or document the naive split's known limitations and add three negative tests that pin the current (buggy) behaviour.

- **[major]** 01-overview.md :: AC 4 — Migrated skill-creator
  - Concern: AC 4.3 / 4.4 list the migrated assets but the plan does not enumerate test cases for the migration itself. The migration of `scripts/run_eval.py`, `scripts/improve_description.py`, etc. replaces every Claude-specific invocation (T3 inventory). Each replacement is a behavioural change that needs a test asserting the Hermes equivalent is used (e.g. shelling out to `hermes` not `claude`, using `HERMES_SESSION` env-var not the Claude nesting guard). The plan should require one test per replaced binding, sourced from the T3 inventory.
  - Fix: In 07-skill-creator-migration.md, list the T3 inventory as a table and for each row enumerate a test that greps the migrated source for the forbidden string (e.g. assert "claude" CLI invocation is absent in `scripts/run_eval.py`) and asserts the Hermes equivalent is present. Add a fixture per script.

- **[major]** 01-overview.md :: AC 4.7 / 4.8 — Tool-name and CLI conventions
  - Concern: AC 4.7 says "Tool-name matches use `tool_name.lower() in (...)`" and AC 4.8 says "CLI invocations use `hermes` not `claude`". The plan does not enumerate a test that scans EVERY migrated file (SKILL.md, agents/*.md, scripts/*.py, eval-viewer/generate_review.py) for the forbidden tokens (`claude`, `Claude`, `CLAUDE`) and asserts zero matches outside of `MIGRATION.md` and bilingual advisories that must reference Claude by name (e.g. "Anthropic skill-creator"). Without a tree-wide grep test, a single missed replacement is undetectable.
  - Fix: Add an integration test `test_migration_no_claude_invocations_remain` that runs `grep -rE "\bclaude\b"` over the migrated skill tree, asserts zero hits in code/commands, and explicitly allows documented mentions (e.g. in MIGRATION.md provenance section).

- **[major]** 01-overview.md :: AC 1.4 / 03-plugin-spec.md §Skill registration
  - Concern: AC 1.4 says the plugin "re-publishes the migrated skill if the user has it installed under `~/.hermes/skills/<cat>/skill-creator/`". The plan does not enumerate a test for the negative case: what if the user has NOT installed the skill? The plan should specify that `register()` is a no-op in that case (and log a bilingual advisory) rather than raise. The coverage targets list "frontmatter present, frontmatter missing → KeyError is caught and logged" but the "skill not installed" path is a different branch (file not found vs. KeyError on the parsed dict) and needs its own test.
  - Fix: Add `test_register_skill_not_installed_is_noop` and `test_register_skill_installed_calls_register_skill`. Split the existing frontmatter-missing test into two: `test_register_frontmatter_missing_logs_and_returns` and the new file-not-found test.

- **[minor]** 01-overview.md :: AC 1.5 — hermes-agent-skill-authoring validation
  - Concern: AC 1.5 says the plugin "passes `hermes-agent-skill-authoring` frontmatter validation" and `tests/integration/test_plugin_manifest.py::test_manifest_conforms_to_hermes_skill_authoring` runs the validator as a subprocess. The plan does not specify what happens if the validator exits non-zero on the plugin's own manifest — is the test expected to fail loudly, or does it skip on missing validator? The latter would silently mask a manifest regression.
  - Fix: Specify the test must hard-fail (no skip) when the validator is absent, or pin the validator's install path in pyproject and assert the dependency is present.

- **[minor]** 03-plugin-spec.md :: plugin.json manifest — requires_env
  - Concern: The manifest declares `requires_env: ["HERMES_SKILL_CREATOR_RAISE_CAP"]`. If Hermes enforces this strictly, the plugin refuses to load when the env var is unset, which contradicts the "default off, opt-in" semantics described in AC 1.2. The plan does not clarify whether `requires_env` is a hard requirement (plugin refuses to load) or a soft hint (logged advisory). This affects testability: a test that loads the plugin without the env var would fail under the strict reading.
  - Fix: Clarify Hermes's `requires_env` semantics (TBD per Q2), and update the manifest to a soft hint if necessary. Add a test `test_plugin_loads_without_env_var` and `test_plugin_logs_advisory_with_env_var`.

- **[minor]** 02-architecture.md :: Failure modes — cache stale
  - Concern: The failure-mode paragraph says Script #2 calls `clear_skills_system_prompt_cache(clear_snapshot=True)` after every successful flip. The plan does not enumerate a test that asserts the cache file (`~/.skills_prompt_snapshot.json`) is actually deleted or invalidated, only that the function is called. A spy test passes even if the function is a no-op in the real implementation.
  - Fix: Add a test that pre-populates the cache file in a fixture HERMES_HOME, runs Script #2 --apply on a profile, and asserts the file is gone (or its mtime is newer).

- **[minor]** 02-architecture.md :: Sequence — graceful degradation
  - Concern: The graceful-degradation paragraph says the plugin emits a "one-time bilingual advisory at session start" when the cap is unpatched. "One-time" is ambiguous: per-session, per-day, per-install? The plan does not enumerate a test for the persistence of the advisory flag (e.g. is it written to `~/.hermes/.hermes_skill_creator_advisory_seen`?). Without a test the branch is un-observable.
  - Fix: Specify the advisory persistence mechanism and add tests: first session emits advisory, second session does not, deleting the persistence file re-emits.

- **[minor]** 00-index.md :: File map — claim of 13 files
  - Concern: The plan claims 13 plan files (00–12) but only 4 are persisted. The summary explicitly states "The StructuredOutput tool returned a validation error and aborted the batch — the files were not persisted." Downstream agents (Phase 5) will read the file map and assume the 9 missing files exist. This is a documentation/reality gap, not a testability gap, but it blocks the testability review of the missing files.
  - Fix: Either (a) re-run the batched write and confirm all 13 files land, or (b) update 00-index.md to reflect reality (4 files written, 9 pending) and add a Phase 4 sub-task to write the remainder.

- **[minor]** 01-overview.md :: AC 4.5 — T3 inventory reference
  - Concern: AC 4.5 says "Every Claude-specific invocation replaced per the T3 inventory" but T3 is not defined in any of the 4 persisted plan files. The T3 inventory lives in `research/` (per the worktree listing) but the plan does not cite the exact file path. A reviewer (or downstream agent) cannot verify AC 4.5 without locating T3.
  - Fix: Add an explicit citation in 07-skill-creator-migration.md (once written) to the T3 inventory file path under `research/`. Cross-link from 01-overview.md.

- **[minor]** 03-plugin-spec.md :: TDD test cases — test_install_idempotent
  - Concern: `test_install_idempotent` asserts the installer is re-runnable and exits 0 with `OK: already installed`. The plan does not enumerate a test for the installer's negative path: what if the target `~/.hermes/skills/<cat>/skill-creator/` is read-only? Or what if a file with the same name exists but is not a skill-creator (collision detection)? 100% branch coverage of the installer is not demonstrable without these.
  - Fix: Add `test_install_collision_detected` (pre-populate target with a file of the same name but different content, assert installer aborts non-zero with bilingual message), `test_install_readonly_target_aborts`, and `test_install_partial_state_rollback` (interrupted mid-copy, assert no partial directory is left behind).

- **[minor]** 01-overview.md :: AC 5.4 — Migration note format
  - Concern: AC 5.4 says Script #1's `--emit-migration-note` regenerates `MIGRATION.md`. The plan does not enumerate a test that asserts the migration note is byte-identical across runs given the same input (determinism), nor a test that asserts the table is exhaustive (every T3 inventory row produces a row in the table). Determinism is critical for downstream re-sync (per migration-fidelity lens).
  - Fix: In 08-migration-note-format.md (once written), add `test_migration_note_deterministic` (run twice, assert byte-identical) and `test_migration_note_exhaustive` (T3 inventory row count == MIGRATION.md table row count).

- **[minor]** 03-plugin-spec.md :: installer.py — installs to ~/.hermes/
  - Concern: The installer copies the plugin to `~/.hermes/plugins/hermes-skill-creator-plugin/` and the skill to `~/.hermes/skills/<cat>/skill-creator/`. The plan does not enumerate a test that the installer respects `HERMES_HOME` override (so it can be tested against a fixture HERMES_HOME without touching the real one). Without this, the integration tests risk writing to the user's actual `~/.hermes/`.
  - Fix: Add `test_install_respects_hermes_home_override` that sets `HERMES_HOME` to a temp dir, runs the installer, and asserts all writes are under the temp dir. Required for safe fixture-based testing.

- **[minor]** 02-architecture.md :: Data flow — patch path step 4
  - Concern: Step 4 says the script "writes the patch to a `.patch` file in the target dir, then applies atomically (write to `.patch.tmp` + rename)". The plan does not enumerate a test for the atomicity claim: simulate a crash between the `.tmp` write and the rename (e.g. by raising inside the rename call), and assert the original file is unchanged and no `.tmp` lingers.
  - Fix: Add `test_apply_atomic_on_rename_failure` to 04-script-1-patch.md TDD list (once written). Use a mock that raises on `Path.rename`.

- **[nit]** 03-plugin-spec.md :: TDD test cases — test_default_flag_off_no_patch
  - Concern: The test name and assertion could be sharpened: "no patch" should be qualified — the original function must still be callable and produce the original 60-char-truncation behaviour. As written, the test could pass with a no-op stub.
  - Fix: Rename to `test_default_flag_off_original_60char_truncation_preserved` and assert a 100-char input is truncated to 60 chars.

- **[nit]** 01-overview.md :: AC 2.9 / 3.9 — bilingual --help
  - Concern: The acceptance criteria state `--help` output is bilingual EN+HU, but no test enumerates the bilingual contract. A test that just runs `--help` and asserts it exits 0 is insufficient.
  - Fix: Add a test `test_help_is_bilingual` that asserts both `[en]` and `[hu]` markers are present in the output, and that every option has both an English description and a Hungarian description.

- **[nit]** 03-plugin-spec.md :: patch_runtime.py — re-binding prompt_builder
  - Concern: The plan rebinds `prompt_builder.extract_skill_description` after importing the module. If `agent/prompt_builder.py` does `from agent.skill_utils import extract_skill_description` at module load, the symbol is already bound at import time and the rebind in `patch_runtime.py` is a no-op (the call site uses the local name). The plan does not test this.
  - Fix: Either (a) verify Hermes's actual import style and document whether the rebind is needed, or (b) patch `agent.skill_utils.extract_skill_description` only and rely on attribute lookup at call time. Add a test that asserts the cap is actually raised end-to-end by simulating a `prompt_builder` call.

- **[nit]** 01-overview.md :: AC 6.1 — files ≤ 500 lines
  - Concern: The acceptance criterion is a constraint on the plan files themselves, not on the implementation files. A pre-commit check that enforces ≤ 500 lines on plan files would help, but the plan does not specify the enforcement mechanism.
  - Fix: Add the ≤ 500 line check to `.pre-commit-config.yaml` as a custom hook or document it as a CI check.

- **[nit]** 02-architecture.md :: Failure modes — patch drift
  - Concern: The plan says LINE_DRIFT triggers a `--force` retry. The plan does not enumerate a test for what happens if `--force` is also failing (e.g. line number changed again between --check and --force). This is a real failure mode worth a test.
  - Fix: Add `test_force_still_drifts_exits_nonzero` (corrupt the file again after the first drift, assert --force also exits non-zero with a clear message).

- **[nit]** 00-index.md :: Hard constraints — bilingual rule
  - Concern: The bilingual rule says "user-facing descriptions and console/log messages bilingual EN+HU". The plan does not enumerate a test that asserts every `print()` / `log.info()` in Scripts #1, #2, and the plugin hooks produces both an `[en]` and a `[hu]` marker. A simple AST-grep test would enforce this.
  - Fix: Add a pre-commit hook or a unit test that walks the AST of every script and asserts that every `print`, `logger.info`, `logger.warning`, `logger.error` call has both an `[en]` and an `[hu]` token in its format string.

- **[nit]** 03-plugin-spec.md :: Resources directory — vendored patched function
  - Concern: The vendored patched function imports `extract_skill_description as _orig` but never calls it. The import is dead code. The plan should either (a) actually use the original as a fallback, or (b) remove the import to keep coverage honest.
  - Fix: Remove the unused import, or refactor to call `_orig` as a fallback (e.g. when the feature flag is off, route to `_orig` instead of returning the original function unchanged).

---

## Lens: safety-and-non-execution — verdict: request-changes

- **[blocker]** 03-plugin-spec.md :: Cap-raise mechanism (declarative, feature-flagged)
  - Concern: The plan describes a runtime monkey-patch of `agent.skill_utils.extract_skill_description` from inside a plugin's `pre_llm_call`/`on_session_start` hook. This violates the project safety rule: NEVER modify ~/.hermes/hermes-agent. A Python-level monkey-patch executed by plugin code mutates the live `agent.skill_utils` module object in the running interpreter, which is the same kind of in-process mutation as editing the .py file on disk. The plan even admits the patch is reverted only when the session ends — meaning during every Hermes session, plugin code is rewriting the behaviour of a module that lives inside the read-only Hermes install. The plan also contradicts itself: the "Hard rule" claims the plugin NEVER writes to ~/.hermes/hermes-agent, but the architecture section also says the cap raise is performed by Script #1 against a user-owned checkout. It must be one or the other, not both, and the runtime monkey-patch path must be removed.
  - Fix: Delete the `patch_runtime.py` / `resources/extract_skill_description_patched.py` runtime monkey-patch entirely. The cap raise must happen ONLY via Script #1 against a user-owned Hermes checkout (not ~/.hermes/hermes-agent). The plugin's `on_session_start` may only emit an advisory if it detects (via read-only static AST scan) that the cap is still 60. Make this a one-time, in-process, side-effect-free check, and explicitly forbid any `setattr` on `agent.skill_utils`.

- **[blocker]** 01-overview.md :: Deliverables §5.2 / Script #1 acceptance criteria 2.8 + 2.9
  - Concern: Acceptance criterion 2.8 says `--task-e-redirect` "applies sites 1–7 from docs/maybe-patch-points.md" and 2.9 says `--help` is bilingual. The plan does not include the actual `MIGRATION.md` or `docs/maybe-patch-points.md` content; without these, the script author cannot know what sites 1–7 are. More importantly, the plan must explicitly forbid Script #1 from defaulting `--target` to `~/.hermes/hermes-agent`. If the operator omits `--target`, the script MUST abort with a clear error rather than guess. The current plan never states this safeguard.
  - Fix: Add to Script #1 spec: (a) `--target` is REQUIRED, not optional, and the script exits non-zero with code 4 (I/O error) if `--target` is unset, points to a non-existent path, points to `~/.hermes/hermes-agent`, or points to a path that does not contain `agent/skill_utils.py`. (b) The script must resolve the target path with `Path.resolve()` and compare against the resolved `~/.hermes/hermes-agent` path; if equal, abort. (c) The bilingual error message must be explicit.

- **[blocker]** 01-overview.md :: Coverage of §5.1..§5.7 + §6.A..§6.E + §8 + §10
  - Concern: The brief requires coverage of §5.1..§5.7 + §6.A..§6.E + §8 + §10. The 4 provided files only cover §5.1 (plugin) and start of §5.2 (Script #1). The summary explicitly admits that 9 of 13 files are missing (04, 05, 06, 07, 08, 09, 10, 11, 12). Without §5.3 (Script #2), §5.4 (migrated skill), §5.5 (migration note), §5.7, §6.A..§6.E, §8 (toolchain/test), §10 (subagent map), the plan is not reviewable end-to-end and the TDD scaffolding cannot begin. This is a structural blocker for the planning phase, not for execution.
  - Fix: Block the planning phase until the remaining 9 files are produced and re-submitted for review. Specifically, the safety reviewer needs to see at minimum: Script #1 full spec, Script #2 full spec, the migrated-skill file mapping, the migration-note format, and the test strategy. Do not proceed to execution.

- **[blocker]** 02-architecture.md :: Plugin install path + Patch path + Profile-manager path
  - Concern: The architecture says the plugin installer "copies the plugin into ~/.hermes/plugins/hermes-skill-creator-plugin/ and the migrated skill into ~/.hermes/skills/<cat>/skill-creator/". Writing to ~/.hermes/ is acceptable for the user-owned skills dir, but the installer must be tested against a fixture HERMES_HOME, never against the real ~/.hermes during CI/dev. The plan does not state that the installer is gated on a confirmation prompt when the target is the real ~/.hermes, nor that the integration tests use a tmp_path-based HERMES_HOME fixture. Without this, an operator accidentally running `python -m hermes_skill_creator_plugin.install` could pollute their live skills dir.
  - Fix: Add to the plugin installer spec: (a) the installer MUST confirm with the operator (TTY prompt) before writing to the real ~/.hermes, defaulting to abort in non-interactive mode unless `--yes` is passed; (b) integration tests MUST set `HERMES_HOME=tmp_path` and assert that no writes reach the real ~/.hermes; (c) the installer MUST refuse to run if `HERMES_HOME` and the resolved `~/.hermes` agree unless `--yes` is passed; (d) the installer's --help is bilingual EN+HU.

- **[major]** 03-plugin-spec.md :: TDD test cases — integration test_install_does_not_modify_hermes_agent
  - Concern: The test `test_install_does_not_modify_hermes_agent` uses a "sentinel: mtime + sha256 of a known file" but does not specify WHICH known file or how the sentinel is established without first reading ~/.hermes/hermes-agent. If the test must read ~/.hermes/hermes-agent to compute a baseline, the test is fragile (Hermes auto-updates could change the file) and the act of reading the live install in test setup is itself a hazard.
  - Fix: Replace the live-sentinel with a fully synthetic sentinel: a directory tree the test creates at tmp_path/hermes-fake-install/ with a known SHA, run the installer against tmp_path/hermes-fake-install, and assert the SHA is unchanged. The test must never open a file under ~/.hermes/hermes-agent. State this explicitly in the test docstring.

- **[major]** 01-overview.md :: Plugin §5.1 acceptance criterion 1.2
  - Concern: AC 1.2 says the plugin "emits a single pre_llm_call advisory if and only if a feature flag is set (default: off)" but the architectural description in 03-plugin-spec.md says the cap-raise is also gated on this flag and the on_session_start hook logs a bilingual advisory on success. The plan does not clarify the relationship between (a) the pre_llm_call advisory, (b) the on_session_start advisory, and (c) the actual cap-raise monkey-patch. If the cap-raise monkey-patch path is removed per the blocker finding above, the remaining advisories need an explicit state machine: which flag controls which advisory, how often each fires, and how to prevent log spam across a long session.
  - Fix: Define a single feature flag matrix: HERMES_SKILL_CREATOR_RAISE_CAP=0 → no advisories, no patches. HERMES_SKILL_CREATOR_RAISE_CAP=1 + cap already raised (detected via AST scan of target checkout) → one-time on_session_start advisory, no pre_llm_call advisory. HERMES_SKILL_CREATOR_RAISE_CAP=1 + cap NOT raised → one-time on_session_start advisory warning the operator to run Script #1, no pre_llm_call advisory. pre_llm_call advisory is removed entirely. Document the state machine in 03-plugin-spec.md.

- **[major]** 02-architecture.md :: Failure modes — Patch drift
  - Concern: Plan says on LINE_DRIFT the operator can re-run with `--force` (line-only override). This is a safety-relevant escape hatch: if the script ever defaults to line-only, or if --force is too easy to invoke, an operator could accidentally apply a patch to the wrong location. The plan must require (a) --force to also re-print the diff, (b) --force to require an explicit `--i-understand-line-drift` confirmation, and (c) --force to be logged to a sidecar audit file. None of these guards are specified.
  - Fix: Add: `--force` requires a second confirmation flag `--i-accept-line-drift`; the script prints the diff and pauses for a TTY confirmation; the patch invocation is appended to `~/.hermes/patch-audit.log` (or a worktree-local equivalent) with timestamp + operator + diff hash. Bilingual prompt. Add a unit test that asserts --force without --i-accept-line-drift exits non-zero.

- **[major]** 03-plugin-spec.md :: Skill registration — i18n messages and KeyError handling
  - Concern: AC 1.4 says the plugin re-registers the migrated skill "if the user has it installed under ~/.hermes/skills/<cat>/skill-creator/". The `register` function in skill_register.py reads SKILL.md and looks up `fm["description"]` directly — if description is missing it raises KeyError. The plan claims this is caught and logged in 100% coverage, but the code shown does not include the try/except. Also, the plan does not specify how the plugin detects "the user has it installed" — does it check ~/.hermes/skills/, or does it trust the bundled skill at src/hermes_skill_creator_plugin/skills/? If the bundled one, then "re-publishes" is misleading; if ~/.hermes/skills/, the plugin must not silently overwrite the user's local copy.
  - Fix: Spell out: (a) the try/except around fm["description"] with bilingual KeyError log; (b) the precedence rule — if ~/.hermes/skills/<cat>/skill-creator/SKILL.md exists, the plugin leaves it untouched and only emits an advisory; if it does not exist, the plugin's installer copies the bundled skill; (c) the `register` function's `ctx.register_skill` call always uses the bundled skill's frontmatter, never reads from ~/.hermes/skills/, to avoid mixing versions.

- **[major]** 01-overview.md :: Script #2 acceptance criteria 3.4
  - Concern: AC 3.4 says `--apply` writes "under set_hermes_home_override AND mirrors os.environ['HERMES_HOME'] per dispute D7". The plan does not specify (a) the exact env-var mutation API (does it use os.environ.__setitem__, or a context manager that restores on exit?), (b) the order of operations between set_hermes_home_override and os.environ mutation, (c) what happens if the two diverge, (d) whether the env mutation persists after the script exits. Without these, the script could leak HERMES_HOME into the operator's shell environment.
  - Fix: Mandate a single `with hermes_home_scope(path):` context manager that (a) sets `set_hermes_home_override(path)`, (b) sets `os.environ['HERMES_HOME'] = str(path)`, (c) yields, (d) on exit, restores both the override and the env var to their pre-entry values via try/finally. Add a unit test that asserts HERMES_HOME is unchanged after the script exits, even on exception. Bilingual.

- **[major]** 02-architecture.md :: Migrated skill runtime path
  - Concern: The architecture says the migrated skill instructs the agent to "call skills_list, then skill_view(name='skill-creator')". The skill-creator invoking its own skill_view to load itself is a circular self-reference: Hermes's skill_view may or may not allow this, and the runtime path is underspecified. Also, the `hermes` CLI is called from the migrated scripts; the plan does not specify how the nesting-guard var (TBD per Q2) is set, nor what happens if the script is invoked outside a Hermes session (e.g., during CI tests).
  - Fix: Replace self-view with a direct read of the bundled SKILL.md: the migrated skill's body should embed the authoring guidance directly, not call skill_view. For the eval scripts' hermes CLI invocations, require that the nesting-guard var (default plan: HERMES_SESSION) is checked before any hermes invocation; if unset, the script exits non-zero with a bilingual error instructing the operator to run from inside a Hermes session. The integration test must cover the unset case.

- **[minor]** 01-overview.md :: Plan files §5.6 acceptance criterion 6.1
  - Concern: AC 6.1 says all files must be ≤ 500 lines. The current files are 68, 109, 147, 147. The missing 9 files must be budgeted to stay under 500 lines; in particular, 04-script-1-patch.md and 07-skill-creator-migration.md are likely to balloon. The plan should declare a per-file line budget now to prevent drift.
  - Fix: Add a per-file line budget table to 00-index.md or 01-overview.md: 04 ≤ 350, 05 ≤ 200, 06 ≤ 300, 07 ≤ 450, 08 ≤ 150, 09 ≤ 250, 10 ≤ 200, 11 ≤ 200, 12 ≤ 250. The reviewer can then enforce.

- **[minor]** 03-plugin-spec.md :: 100% code + logic coverage targets
  - Concern: Plan claims 100% code + logic coverage is achievable but does not address coverage of the exception paths in patch_runtime.py / skill_register.py when the feature flag is set but `agent.skill_utils` is not importable (e.g., plugin installed in a non-Hermes Python environment). The test matrix should include a test where the import is mocked to raise ImportError and the function returns False cleanly.
  - Fix: Add test case: `test_flag_on_but_module_unimportable` — `agent.skill_utils` import raises ImportError → `maybe_apply_cap_raise` returns False and logs a bilingual warning. Same for `prompt_builder` import.

- **[minor]** 02-architecture.md :: Sequence — graceful degradation when patch not applied
  - Concern: Plan says the migrated skill ships a `description_60char` secondary field, but the frontmatter spec in 03-plugin-spec.md / 01-overview.md AC 4.2 says the description is a single string ≤ 1024 chars. The two descriptions are not reconciled. If the cap is 60, the primary `description` field is silently truncated by Hermes and the agent never sees the full guidance. The fallback mechanism is therefore broken by construction.
  - Fix: Either (a) drop the `description_60char` field entirely and document that the skill is only fully functional on patched Hermes, or (b) put the 60-char summary in `metadata.hermes.short_description` and instruct the agent body to read both fields, and add a test that the frontmatter validates with both fields present. Pick one and remove the other from the spec.

- **[minor]** 01-overview.md :: Bilingual rule consistency
  - Concern: AC 2.9, 3.9 say `--help` is bilingual EN+HU. The legend in 00-index.md uses the format `[en] ... / [hu] ...`. The plan does not specify which bilingual format is used for `print()` vs `--help` vs log lines, nor whether the bilingual help is rendered as two columns, two sequential lines, or concatenated. The TDD tests should pin the exact format.
  - Fix: Define a single bilingual format spec: console messages use `[en] text / [hu] szöveg` on a single line; --help uses two top-level sections ("Usage (English)" and "Használat (magyar)") with mirrored content. Add a test that asserts the exact format via a snapshot fixture.

- **[minor]** 00-index.md :: File map
  - Concern: The file map lists 13 files but the section heading says "13 files" while the entries only enumerate 13 rows (00–12). The plan also claims to cover §5.1..§5.5 + §5.6 (the plan files themselves), but does not list §5.7. Either §5.7 is implicit in §5.6, or it is missing. The brief requires §5.7 coverage.
  - Fix: Add a row to the file map for §5.7, or explicitly state in 01-overview.md that §5.7 is "out of scope for this plan" with a justification. If out of scope, escalate at the HITL gate.

- **[minor]** 02-architecture.md :: Plugin install path
  - Concern: Plan says the plugin installer is itself idempotent but does not specify how idempotency is verified. If the installer's idempotency check is mtime-based, the second run could be a no-op even if the skill is corrupted. If sha256-based, the check is robust but the plan does not say.
  - Fix: Mandate sha256-based idempotency: the installer records the sha256 of every file it writes in `~/.hermes/plugins/hermes-skill-creator-plugin/.install-manifest.json`; the second run re-hashes and skips files whose sha256 matches. Add a test that asserts the second run exits 0 with `OK: already installed` and writes zero bytes.

- **[nit]** 03-plugin-spec.md :: plugin.json manifest
  - Concern: Manifest's `description` field exceeds 1024 chars after the cap raise — actually it is well under, but the manifest's `kind=skill_authoring` is not a documented Hermes kind. Confirm against `hermes-agent-skill-authoring` validator before relying on it.
  - Fix: Mark `kind=skill_authoring` as a TBD confirmed against the live validator at planning time (Q-open in summary). The integration test must run the actual validator.

- **[nit]** 01-overview.md :: Acceptance criteria numbering
  - Concern: Acceptance criteria use 1.1, 1.2 (decimal), but the bullet list uses `-1.1` which renders inconsistently. Some markdown renderers will treat `-1.1` as a list item, not as a numbered AC.
  - Fix: Use unambiguous numbering: `AC-1.1`, `AC-1.2`, etc. Update throughout.

- **[nit]** 02-architecture.md :: Component diagram
  - Concern: The component diagram labels `agent/prompt_builder.py [:1070-1090]` and `agent/skill_utils.py [:647-655]` but the call-site is `:1090` while the function-def range is `:647-655`. The two ranges are not the same file. The diagram should disambiguate: which is the patch site (the function def) and which is the call site (prompt_builder).
  - Fix: Label: `[patch target: agent/skill_utils.py:647-655]` and `[call site: agent/prompt_builder.py:1070-1090]`. Make the relationship explicit.

- **[nit]** 03-plugin-spec.md :: Feature flag name
  - Concern: Feature flag is `HERMES_SKILL_CREATOR_RAISE_CAP` but the name implies it raises the cap. If the runtime monkey-patch is removed per the blocker finding, the flag should be renamed to reflect its actual role (advisory-only).
  - Fix: Rename to `HERMES_SKILL_CREATOR_PLUGIN=1` (advisory) once the runtime patch is removed, or keep the old name but document that the flag now only controls advisories.

---

## Lens: migration-fidelity — verdict: request-changes

- **[major]** 01-overview.md :: §5.4 acceptance criteria 4.5 — "Every Claude-specific invocation replaced per the T3 inventory"
  - Concern: Plan invokes a "T3 inventory" that is never actually enumerated in any of the 4 emitted files. Cross-referencing the vendored `research/anthropic-skill-creator-original/skills/skill-creator/SKILL.md` + `scripts/*.py` + `agents/*.md`, the following Claude-specific bindings are present and NOT individually mapped to a Hermes equivalent anywhere in the plan: (a) `claude -p --output-format text` in `improve_description.py:26`, (b) `claude -p --output-format stream-json --verbose` in `run_eval.py:71-87`, (c) `CLAUDECODE` env-var un-nesting in both `improve_description.py:33` and `run_eval.py:83` (Hermes's equivalent is TBD per Q2), (d) the `--model` flag, (e) `.claude/commands/` command-file fabrication so that the eval target shows up in the available_skills list (`run_eval.py:45-53`), (f) `claude.ai` and `Cowork`-specific sections in SKILL.md lines 432, 445-455, 483, (g) `os.environ["CLAUDECODE"]` removal pattern, (h) the entire "if not webbrowser.open()" Cowork fallback at line 247. Plan file 07 is not yet emitted, but 01's AC 4.5 cannot be verified without a per-binding mapping table. The plan must list each binding, its Hermes replacement, and the test that proves the replacement fires.
  - Fix: Add an explicit "T3 inventory table" in `01-overview.md` (or a new file `07a-binding-inventory.md`) with columns: `path:line | claude-binding | hermes-binding | test-id | migration-note-line`. Cover at minimum: claude -p, --output-format, CLAUDECODE env-var, .claude/commands/ fabrication, claude.ai/Cowork sections, --model flag, webbrowser fallback, --verbose, --include-partial-messages. AC 4.5 then becomes "every row in this table has a passing test."

- **[major]** 03-plugin-spec.md :: Plugin layout — `extract_skill_description_patched.py` monkey-patch mechanism
  - Concern: The plugin spec defines a runtime Python-level monkey-patch of `agent.skill_utils.extract_skill_description` and `agent.prompt_builder.extract_skill_description`. This directly contradicts the brief's "Hermes is v0.16.0 at commit 368fcf1ff, READ-ONLY" safety stance and the open question Q10 (which considers a per-profile patch against a config snapshot, not a runtime monkey-patch against the installed Python module). A monkey-patch against `~/.hermes/hermes-agent` is effectively modifying the live Hermes install in-process — even if no file is written, the running interpreter's bound function is mutated, which is what the safety rule prohibits. Additionally, the patch is documented as "reverted when the session ends" but the rebinding to `agent.prompt_builder.extract_skill_description` is module-level, so subsequent `import prompt_builder` calls in any subprocess or background task WILL see the patched function for the lifetime of the process — the "revert" is a fiction. This also breaks the brief's §5.2 / §6.B story: Script #1 exists to raise the cap as a source edit; the plugin monkey-patch is a parallel, competing mechanism. The plan must pick ONE cap-raise mechanism.
  - Fix: Either (a) drop the runtime monkey-patch and make the plugin purely advisory: on session start, log a bilingual warning if cap is unraised and prompt the operator to run Script #1, OR (b) drop Script #1's cap-raise site and make the plugin the SOLE cap-raise mechanism (and have it ship a vendored patched module that the operator applies to a user-owned Hermes checkout via `python -m hermes_skill_creator_plugin.apply_patch --target <checkout>`). Pick (a) and explicitly deprecate the runtime monkey-patch; update AC 1.2, 1.3, and the §03 hook signatures accordingly.

- **[major]** 01-overview.md :: Deliverable table — §5.4 row, plus the entire "Claude strengths preserved" assertion
  - Concern: Plan claims "Claude strengths preserved (subagent split, eval pipeline, eval viewer)" but does not define WHAT makes each strength "preserved" — i.e., what the behavioral acceptance criterion is for a Hermes-native evaluation pipeline. Specifically: (1) the vendored `agents/{grader,analyzer,comparator}.md` are YAML-frontmatter sub-agent definitions in Anthropic's format; the plan says they will be "preserved with the same semantic roles" and "lowercased per Hermes convention" — but does not show what the Hermes-side YAML/registration format looks like, nor how the agents are wired into Hermes's sub-agent dispatch (Hermes has its own sub-agent system with its own `agent_name`/spawn conventions; mapping is not 1:1). (2) The eval pipeline `scripts/run_eval.py` fabricates `.claude/commands/` so the eval target appears in the available_skills list; the Hermes equivalent is to write into `~/.hermes/skills/<cat>/<eval-target>/SKILL.md` — but the plan does not enumerate this. (3) The "eval viewer" is HTML+JS that the LLM serves via `generate_review.py --static`; there is no assertion that Hermes can host a local HTTP server from a skill body. None of these three strengths is spec'd at implementation level.
  - Fix: In file 07 (when emitted), add a "Strength preservation matrix" with three rows (subagent, eval pipeline, eval viewer) and per-row acceptance criteria: e.g., "After migration, `hermes agent spawn grader --eval-id X` returns a grading report in the same JSON shape as Anthropic's grader agent." For the eval viewer, add an AC that `generate_review.py` produces an HTML file openable via `file://` and that the HTML's JS reads `feedback.json` from the expected Hermes path. Reference the actual Hermes sub-agent registration API (which must be read out of `~/.hermes/hermes-agent/agent_registry/` or equivalent before file 07 is written — this is open question material).

- **[major]** 01-overview.md :: Acceptance criterion 4.6 — "Env-var nesting guard updated to Hermes's nesting-guard var (TBD per Q2; default plan: HERMES_SESSION)"
  - Concern: Plan defers the nesting-guard var name to Q2 (open question), but the vendored scripts `improve_description.py:33` and `run_eval.py:83` literally do `env = {k: v for k, v in os.environ.items() if k != "CLAUDECODE"}` and `os.environ.pop("CLAUDECODE", None)`. This is not a "TBD" — it is a load-bearing behavioral binding. The migrated `run_eval.py` MUST un-set Hermes's nesting-guard var to allow `hermes -p` to nest under the parent Hermes session, and MUST re-set it on exit (or scope the unset to the subprocess env only). The plan also does not specify the BEHAVIOR on non-Hermes hosts (e.g., is the migrated script a no-op if the env var is unset, or does it fail?). And AC 4.6 doesn't list a test case for the env-var un-nest — so even if the binding is implemented, coverage will be partial. The Hermes nesting-guard var name MUST be confirmed by reading `~/.hermes/hermes-agent` BEFORE file 07 is emitted, not deferred to Phase 5.
  - Fix: Escalate Q2 to BLOCKER for file 07 emission. In file 01 AC 4.6, add test cases: `test_run_eval_unnests_hermes_guard`, `test_run_eval_restores_hermes_guard_on_exit`, `test_run_eval_no_op_when_guard_unset`, `test_improve_description_unnests_hermes_guard`. Add AC: "The migrated `run_eval.py` and `improve_description.py` import a single `hermes_subprocess_env()` helper from `hermes_skill_creator_plugin._subprocess` that returns a copy of `os.environ` with the Hermes nesting-guard var removed; this helper is the ONLY place the var name is referenced (single source of truth)."

- **[major]** 01-overview.md :: Acceptance criterion 4.9 — "Eval viewer generate_review.py updated to read Hermes-style streaming JSON event shape (TBD per Q3)"
  - Concern: The vendored `run_eval.py:71-87` invokes `claude -p --output-format stream-json` and parses the resulting NDJSON event stream. The Hermes CLI's equivalent event shape is TBD per Q3 (open question), and the plan defers this to "Phase 5." But AC 4.9 is the acceptance criterion for file 07 (the migrated skill), and the eval pipeline is the heart of the skill's "strength preservation" claim (per finding #3 above). If the Hermes event shape differs from Anthropic's `stream-json` (e.g., field name `type` vs `event`, `message.content[0].text` vs `content[0].input.text`, `session_id` vs `conversation_id`), the entire `aggregate_benchmark.py` + `generate_report.py` chain must be re-mapped. The plan does not show a translator table, nor does it have an integration test that exercises the Hermes event-shape end-to-end. Without this, AC 4.9 is unverifiable.
  - Fix: Promote Q3 to BLOCKER for file 07 emission. Add a "Hermes event-shape translator" section to file 07 (when emitted) showing: `claude: {type: "assistant", message: {content: [{type: "text", text: "..."}]}}` -> `hermes: {event: "message", role: "assistant", content: "..."}` (or whatever the real shape is, to be confirmed by reading `~/.hermes/hermes-agent/streaming/`). Add integration test: `test_aggregate_benchmark_parses_hermes_stream_json` that runs the actual Hermes CLI (in a sandboxed fixture) and feeds its NDJSON output through the migrated aggregator. Coverage without a real Hermes event fixture is impossible — flag this in §9 test strategy.

- **[major]** 01-overview.md :: Acceptance criterion 5.x — Migration note format
  - Concern: Plan says the migration note is emitted by Script #1 in `--emit-migration-note` mode. But Script #1's patch sites are the cap-raise site in `agent/skill_utils.py` and the 7 Task E sites in `agent/prompt_builder.py`, `agent/background_review.py`, `tools/skill_manager_tool.py`, `website/docs/user-guide/features/skills.md`. The migration note is supposed to enumerate "every Claude-specific invocation replacement" per §5.5 — but the patched Hermes files contain ZERO Claude-specific invocations; they are Hermes files. The Claude-specific invocations live in the vendored skill (file 07's output), not in the Hermes install. So who emits the migration note for file 07's bindings? The plan is silent. Either (a) the migration note is split into two: one for Script #1's Hermes-side patch, and one for file 07's vendored-skill migration; or (b) the migration note is a single file emitted at the end of the migration by a third mechanism. The plan must say which.
  - Fix: Split the migration note into two paths. (a) `MIGRATION.hermes-patch.md` — emitted by Script #1 `--emit-migration-note`; covers only Script #1's patch sites (cap raise + 7 Task E sites). (b) `MIGRATION.skill-port.md` — emitted by a new `scripts/script_3_emit_skill_migration_note.py` (or by Script #1 with a new `--emit-skill-migration-note` flag) that walks the migrated skill tree and emits a row per Claude-binding-replacement. Update AC 5.1 to require both files. Add a top-level `MIGRATION.md` that indexes both. This also resolves the "authoritative artifact for downstream AI agents" claim — the index file is the entry point.

- **[minor]** 01-overview.md :: Acceptance criterion 4.7 — "Tool-name matches use `tool_name.lower() in (...)`"
  - Concern: Plan asserts Hermes tool names are lowercase but does not cite the source for this claim. The vendored SKILL.md does not appear to use `tool_name` matching directly (I grep'd and got no hits), so this AC may be a forward-looking migration requirement rather than a direct binding replacement. If the migrated skill body needs to teach the LLM that Hermes tool names are lowercase (e.g., `read` vs `Read`, `write` vs `Write`, `bash` vs `Bash`), the AC needs an explicit list of the lowercase names with their uppercase-Anthropic equivalents, AND a test that the migrated SKILL.md contains the right names. The plan does not list this table.
  - Fix: Add a "Hermes tool name mapping" table to file 07 (when emitted): `Anthropic: Read, Write, Edit, Bash, Glob, Grep, WebFetch, WebSearch -> Hermes: read, write, edit, bash, glob, grep, web_fetch, web_search` (or whatever the real mappings are, to be confirmed by reading `~/.hermes/hermes-agent/tools/`). Add AC 4.7.1: "migrated SKILL.md contains no uppercase tool name outside of code-fence blocks" (verifiable via a static scan test).

- **[minor]** 01-overview.md :: Acceptance criterion 4.2 — `metadata.hermes.tags=[authoring, validation, eval, migration]`
  - Concern: The vendored SKILL.md does NOT have a `metadata.hermes.tags` field — it has Anthropic-specific frontmatter. The plan implies the migrated SKILL.md's frontmatter will be rewritten in Hermes's validator-compatible shape. But the file at 01-overview.md does not show the full proposed frontmatter for the migrated skill; only the `name`, `description`, `tags`, and `related_skills` are mentioned. A complete AC 4.2 would also list: `version`, `allowed-tools` (Hermes equivalent of Anthropic's `allowed-tools` — is it a list, a single string, or absent?), `model` (Anthropic skills can pin a model; can Hermes skills?), and any Hermes-specific body directives (e.g., how the skill body references sub-agents in Hermes's syntax vs. Anthropic's). Without the full frontmatter, the validator-conformance test in 03 cannot be designed.
  - Fix: Add to file 07 (when emitted) the full proposed frontmatter block for the migrated skill, with a line-by-line justification for each field (e.g., "model omitted — Hermes skill-authoring validator rejects pinned model per tools/skill_manager_tool.py:_validate_frontmatter rule R-X"). Add an AC: "the migrated SKILL.md frontmatter passes the hermes-agent-skill-authoring validator with the same rules as the plugin's own SKILL.md."

- **[minor]** 03-plugin-spec.md :: Plugin layout — `tests/integration/test_plugin_install.py::test_install_does_not_modify_hermes_agent`
  - Concern: Test asserts `~/.hermes/hermes-agent/` is unchanged after install (sentinel: mtime + sha256 of a known file). But the `installer.py` is described as "copies the plugin into `~/.hermes/plugins/hermes-skill-creator-plugin/`" — that path is INSIDE `~/.hermes/`, not `~/.hermes/hermes-agent/`. The test name is misleading: it should be `test_install_does_not_modify_hermes_agent_checkout` and the sentinel should be the Hermes source tree, not the entire `~/.hermes/` dir. Also, the installer's destination path is `~/.hermes/plugins/...` — does this match the Hermes plugin discovery convention? The plan does not cite the convention. If the convention is `~/.hermes/hermes-agent/plugins/` (i.e., inside the Hermes install), the installer's destination is wrong AND the safety rule is violated.
  - Fix: In file 03, add a paragraph citing the exact plugin discovery convention (read from `~/.hermes/hermes-agent/` plugin loader code) and the exact install destination path. Update the test name to `test_install_does_not_modify_hermes_agent_checkout` and add a second sentinel: `test_install_does_not_modify_hermes_agent_source_files` that checks the sha256 of the vendored Hermes Python files (e.g., `agent/skill_utils.py`) is unchanged.

- **[minor]** 02-architecture.md :: Component diagram — `scripts/script_1_patch.py` and `scripts/script_2_profiles.py` placement
  - Concern: The diagram places both scripts at the worktree root, but plan 01's AC 2.x and 3.x say the scripts must be `uv run python` invokable from a `uv venv` environment. If both scripts live at the worktree root and the worktree is also the Python package source (per the plugin layout in 03), there is a path conflict: importing `hermes_skill_creator_plugin` from the script will resolve to the worktree's source tree, not the installed venv copy. The plan needs a clean separation: e.g., `src/hermes_skill_creator_plugin/` for the package, `scripts/` for the entry points, and a `pyproject.toml` with `[project.scripts]` entries that expose `hermes-skill-creator-patch` and `hermes-skill-creator-profiles` console scripts.
  - Fix: Update file 02's diagram to show the package at `src/hermes_skill_creator_plugin/` (matching file 03's layout) and the entry points under `scripts/` with a `[project.scripts]` block in `pyproject.toml`. Add AC: "Each entry point is invokable as `uv run <console-script-name> --help` from a fresh `uv venv` and emits bilingual help text." Add a test: `tests/integration/test_console_scripts.py::test_both_console_scripts_installed` that runs `uv run <name> --help` and asserts exit 0 + bilingual output.

- **[minor]** 01-overview.md :: Acceptance criterion 1.4 — "plugin exposes register_skill('skill-creator', ...)"
  - Concern: AC 1.4 says the plugin re-publishes the migrated skill if the user has it installed under `~/.hermes/skills/<cat>/skill-creator/`. But file 03's `skill_register.py` does NOT do that — it reads from `Path(__file__).parent / "skills" / "skill-creator"`, i.e., from the plugin's OWN bundled copy, not from the user's `~/.hermes/skills/` install. The "re-publishes the migrated skill if the user has it installed" behavior is not implemented in 03's spec. This is a discrepancy between the AC and the implementation spec — pick one.
  - Fix: Decide: (a) the plugin re-publishes from its own bundled copy (current 03 spec) — change AC 1.4 to match; OR (b) the plugin re-publishes from the user's `~/.hermes/skills/<cat>/skill-creator/` (current 01 AC) — change file 03's `skill_register.py` to walk `~/.hermes/skills/` and prefer the user's copy over the bundled one, with a fallback to the bundled copy. Document the precedence rule.

- **[minor]** 02-architecture.md :: "Failure modes" — "Patch drift -> LINE_DRIFT diagnostic; operator runs --force to retry line-only"
  - Concern: This is correct, but the plan does not specify what `--force` does on a SUCCESSFUL first match. Concretely: if the script finds the text+line match in 6/7 sites and line-drift in 1/7, does `--force` retry just the drifted site, or all 7? If all 7, the operator could be silently re-applying an already-patched site (which the script must handle as no-op, per AC 2.1). The plan should specify that `--force` retries ONLY the drifted sites, and that a successful retry on a drifted site does NOT re-apply already-matched sites. Otherwise the script's idempotency story is incomplete.
  - Fix: In file 04 (when emitted), add: "`--force` retries only sites with LINE_DRIFT diagnostics. Sites that already matched on a prior run are NOT re-applied. The script tracks per-site state in a `.patch.state.json` sidecar; `--force --resume` reads the sidecar and skips matched sites." Add a test: `test_force_retries_only_drifted_sites` that sets up a fixture where 6/7 sites are pre-patched and 1/7 has line drift, runs `--force`, and asserts the 6 already-patched sites are untouched (sha256 unchanged) and the drifted site is patched.

- **[nit]** 01-overview.md :: Acceptance criterion 2.10 — exit code table
  - Concern: Exit code 4 is "I/O error" and exit code 5 is "user abort". The plan does not specify what "user abort" means in a non-interactive script (i.e., with no TTY). If `--check` is non-interactive and the user has not signaled anything, exit code 5 is dead. Clarify that exit code 5 is reserved for an interactive confirmation prompt that the script can opt to skip via `--yes`.
  - Fix: Add a row to the exit code table: "5 = user abort (only raised by interactive confirmation prompt; --yes suppresses the prompt and makes exit 5 unreachable)."

- **[nit]** 03-plugin-spec.md :: Plugin manifest `requires_env: ["HERMES_SKILL_CREATOR_RAISE_CAP"]`
  - Concern: Manifest declares the env var as `requires_env` but the `on_session_start` hook in the same file guards the patch on `os.environ.get("HERMES_SKILL_CREATOR_RAISE_CAP", "") != "1"`. If the manifest validator requires the env var to be set at install time, the install will fail for any user who has not set it. Clarify whether `requires_env` means "must be set" or "may be set" (i.e., declared for documentation only). The convention is plugin-specific and the plan does not cite the convention.
  - Fix: Either (a) change the manifest to omit `requires_env` (treat as documentation only, mention in the plugin description) and add a paragraph in file 03 explaining the env var is opt-in, OR (b) keep `requires_env` and add an AC: "installer.py sets HERMES_SKILL_CREATOR_RAISE_CAP=0 in a sourced env file if the user has not set it." Pick (a) — opt-in env vars are the simpler contract.

- **[nit]** 00-index.md :: File map — 8 of 13 files listed as not yet emitted
  - Concern: Index file references 13 files but the summary at the top of this review says only 4 were emitted. The reader following the index will be confused. The index should be re-emitted AFTER all 13 files exist, or should mark un-emitted files as `[pending — see 12-risks-and-open-questions.md]` so the reader knows to wait.
  - Fix: Add a "Status legend" to the file map: `[emitted]`, `[pending — awaiting StructuredOutput retry]`. Mark files 04-12 as `[pending]` until the retry succeeds. Re-emit 00-index.md as the LAST file in the batch.

- **[nit]** 03-plugin-spec.md :: TDD test cases — `test_extract_skill_description_patched.py::test_zwj_emoji_no_split_invariant`
  - Concern: Test asserts ZWJ-emoji sequence of length 5 stays intact when `len(desc) == 1024` and "verifies the slice is on code points, not graphemes; documented behaviour, not a bug." This is a test of an UNDOCUMENTED behavior with a comment that contradicts the test name ("no_split_invariant"). A test that documents "this is not a bug" is a smell — either fix the bug (slice on graphemes) or remove the test. The current spec effectively encodes the bug.
  - Fix: Remove the test, or rename it to `test_zwj_emoji_known_limitation_slice_on_codepoints` and add a corresponding issue link in `12-risks-and-open-questions.md` (e.g., R11: "Description truncation slices on Unicode code points, may split ZWJ-emoji sequences. Acceptable for ASCII-heavy skill descriptions; flag for follow-up if i18n skill descriptions include emoji.").

---

