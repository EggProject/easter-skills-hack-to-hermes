# Phase 5 előtti 4-lencses adversarial review (V2)

**Overall verdict**: block

**Reviews**: 4 | **Blockers**: 9 | **Findings**: 56

## Per-lens verdicts

### completeness-vs-brief — verdict: **approve**

- Blockers: 0
- Findings: 8

**[minor]** 08-migration-note-format.md :: AC-5.5 / MIGRATION.skill-port.md table

  - Concern: Plan commits to exactly 15 rows in MIGRATION.skill-port.md (matching the T3 inventory in 07). However, _research/anthropicCreator.json enumerates ~30 claudeSpecificInvocations across SKILL.md, scripts/*.py, agents/*.md, and quick_validate.py. The plan's T3 is a curated subset (eval scripts' Claude CLI invocations + Cowork sections + claude.ai URLs + env-var guards + .claude/commands/ fabrication + tool-name PascalCase + subagent 'agent' -> 'subagent' renames). A reviewer reading both files will not see an explicit justification for the subset selection. 12 OOS and 01 AC-4.5 say 'every Claude-specific invocation replaced per the T3 inventory' but the T3 itself is narrower than the full binding list. A downstream AI agent reading the migration note may miss bindings that are not in the 15-row table but ARE in the vendored source.
  - Fix: Add a one-line scope note in 07 T3 inventory preamble: 'T3 enumerates the 15 highest-risk runtime-binding replacements (eval-pipeline invocations, env-var guards, Cowork sections, and the .claude/commands/ staging dir). Cosmetic prose-only edits and module-docstring edits are ported verbatim by the migration scripts and are tracked in the eval script's commit log, not in the migration note.' Reference _research/anthropicCreator.json as the full binding registry.

**[minor]** 03-plugin-spec.md :: plugin.json manifest / AC-1.1 kind=skill_authoring

  - Concern: AC-1.1 pins `kind=skill_authoring` in the plugin manifest. AC-1.5 separately commits the plugin's own SKILL.md to pass `hermes-agent-skill-authoring` frontmatter validation. The two are different things: `kind` is a plugin manifest field (per pluginAuthoring.json, accepted values are unverified); `hermes-agent-skill-authoring` is a skill in the skills/ tree that runs the frontmatter validator. A brief reader can confuse the two. 03 §Fix ledger acknowledges this: 'kind=skill_authoring — pinned (TBD confirmation deferred to 12-Q-TBD; integration test runs the actual validator)'. 12 has no Q-TBD. The TBD is documented but not escalated for HITL.
  - Fix: Either (a) add a Q10 in 12 stating 'Confirm kind=skill_authoring is a valid value in the Hermes v0.16.0 plugin manifest schema' with a default of 'accept; integration test verifies', or (b) drop the value and use a documented kind. Option (a) is consistent with the rest of the plan.

**[minor]** 12-risks-and-open-questions.md :: Out of scope (explicit) / system_prompt.py

  - Concern: 12 OOS line 115 says 'agent/system_prompt.py is NOT a patch site. It is a consumer of MEMORY_GUIDANCE / SKILLS_GUIDANCE from prompt_builder. Patching prompt_builder is sufficient.' This is consistent with 05 (which lists 7 sites) and the Diagnose refuted-claim D8. However, _research/taskECandidates.json enumerates system_prompt.py:117 tool_guidance assembly as a `sourceOfTruth=merged` site with a comment-only edit (no text change). The research JSON calls for a synchronization-contract comment at the system_prompt.py tool_guidance.append sites. The plan does not include this comment-only site. It is a 1-line comment in a Python file, not a runtime behavior change, so omission is low-impact.
  - Fix: Add a one-line note to 12 OOS: 'Per taskECandidates.json independentCandidates site 6, system_prompt.py:117 carries a comment-only synchronization marker (no text change). Not in the 7-site Task E spec; not a Script #1 patch site. Optional Phase 5 cleanup.' This makes the research/plan reconciliation explicit.

**[nit]** 04-script-1-patch.md :: AC-2.2 / AC-2.3 / AC-2.4 / AC-2.5.1

  - Concern: V1 fix verified — all three V1 blockers on Script #1 are addressed: AC-2.4 rewritten (exit 2, no auto-bypass), AC-2.5.1 added (--force needs --i-accept-line-drift + TTY confirm + audit log), atomic write protocol fully specified. Re-derivable from _research/truncation.json.

**[nit]** 03-plugin-spec.md :: Cap-raise mechanism (static-AST, NOT runtime)

  - Concern: V1 fix verified — runtime monkey-patch deleted; plugin is purely declarative + advisory. Cap-raise is Script #1's sole responsibility. Re-derivable from _research/pluginAuthoring.json T2 ('no clean plugin hook to monkey-patch a hardcoded constant').

**[nit]** 06-script-2-profiles.md :: AC-3.4 / AC-3.6 / hermes_home_scope

  - Concern: V1 fix verified — hermes_home_scope mirrors BOTH set_hermes_home_override AND os.environ['HERMES_HOME'] in try/finally. load_config() and save_config() called without path= arg (D4). get_disabled_skill_names(platform=None) used from agent.skill_utils, not get_disabled_skills (D5). _PROFILE_DIRS correctly enumerated, gateway.pid as flat file (D6). do_install inside scope (D7).

**[nit]** 08-migration-note-format.md :: AC-5.1 / 3-file split

  - Concern: V1 fix verified — MIGRATION split into 3 source-controlled files. Re-derivable from _diagnose.md D10 refuted-claim.

**[nit]** 05-script-1-task-e-toggle.md :: 7-site Task E spec

  - Concern: V1 fix verified — 05 lists all 7 Task E sites with site_id, file:line, 8+ char anchor, replacement_text, line shift. system_prompt.py explicitly excluded. E6 OPTIONAL with --no-schema-redirect bypass. Re-derivable from _research/taskECandidates.json reconciledFinalSet.

---

### testability-and-coverage — verdict: **request-changes**

- Blockers: 3
- Findings: 19

**[BLOCKER]** 09-test-strategy.md :: Fixture strategy / conftest.py hermes_checkout

  - Concern: The `hermes_checkout` fixture calls `seed_minimal(checkout)` from `tests/fixtures/minimal_hermes/seed_minimal.py`. The plan never defines what `seed_minimal` writes, never specifies that the fixture seeds the EXACT text used by every anchor (e.g. `if len(desc) > 60:` at line 649, `SKILLS_GUIDANCE = """` at line 172, `MEMORY_GUIDANCE = """` at line 143, `_SKILL_REVIEW_PROMPT = """` at line 45 with inner-line 100 anchor `4. Use the Skill tool`, etc.). Without that contract the test cannot determine that a freshly-seeded fixture will satisfy Script #1's multi-signal (text+line) targeting. This is a BLOCKER because the TDD-list claim "100% code+logic coverage" cannot be exercised without a fixture whose anchors exactly match the patcher — and the plan never pins the fixture's anchor bytes. Suggested fix: Add a `tests/fixtures/minimal_hermes/seed_minimal.py` spec section (or pin it in §09) listing every byte that must be laid down, e.g. the S1.cap file content with the unpatched `if len(desc) > 60:` at exactly line 649, the SKILLS_GUIDANCE block with the literal `SKILLS_GUIDANCE = """` on line 172, etc., and write `test_seed_minimal_anchors_match_patch_sites` that asserts byte-identity.
  - Fix: Add a "Seed-minimal-fixture contract" subsection to 09-test-strategy.md enumerating each anchor file's required line+text, plus a `test_seed_minimal_matches_patch_anchors` meta-test that runs Script #1's anchor-finder against the seed and asserts every site resolves with zero drift.

**[BLOCKER]** 03-plugin-spec.md :: plugin.json manifest / TDD test list - manifest validation

  - Concern: AC-1.1 and AC-1.5 require the plugin manifest to pass the `hermes-agent-skill-authoring` validator. AC-1.1 says "run as a subprocess test", but the TDD test list for `_advisory`/`skill_register`/`installer`/`hooks` in 03-plugin-spec.md never lists a test that invokes the actual validator (`tools/skill_manager_tool.py:_validate_frontmatter`) against the bundled `plugin.json` or `SKILL.md`. The only related test is `test_frontmatter_passes_hermes_validator` in 07, which validates SKILL.md only. There's no analogous `test_plugin_manifest_passes_validator` or `test_install_loads_via_plugin_manager` that runs the real `_parse_manifest` + `_load_plugin` from `hermes_cli/plugins.py` against the bundled `plugin.json`. Because the plan relies on subprocess-isolation from `~/.hermes/hermes-agent`, this is the only way to verify manifest acceptance without touching the live install — and the test list does not include it. This is a blocker for AC-1.1/AC-1.5 coverage.
  - Fix: Add to 03-plugin-spec.md TDD section: `test_plugin_manifest_passes_hermes_parser` — invokes `hermes_cli.plugins._parse_manifest(bundled_plugin_json)` (in-process import) and asserts the parsed PluginManifest has the expected name/version/kind. Add `test_skills_dir_layout_passes_loader` — invokes `tools/skills_tool._serve_plugin_skill` (or `_is_plugin_skill_directory`) against the bundled `skills/skill-creator/` and asserts it loads without error. Both tests run in-process and never touch `~/.hermes/hermes-agent`.

**[BLOCKER]** 06-script-2-profiles.md :: hermes_home_scope context manager - in-process override token

  - Concern: The `hermes_home_scope` context-manager sketch calls `get_current_override_token()` (a helper that does not exist in `hermes_constants`). The real API per `~/.hermes/hermes-agent/hermes_constants.py:21-28` is `set_hermes_home_override(path) -> Token`, and the caller is expected to hold the returned Token and pass it to `reset_hermes_home_override(token)` on exit. The plan's snippet stores `prev_override_token = get_current_override_token()` and passes that to `reset_hermes_home_override(prev_override_token)`. This API mismatch is testability-impacting because the test `test_set_hermes_home_override_called` and `test_scope_restores_on_exception` cannot pass without the helper being correct. The plan also does not include a TDD test that asserts the Token returned by `set_hermes_home_override` is the one passed to `reset_hermes_home_override` on exit (i.e. true round-trip restoration). Without that test, coverage cannot catch a leak.
  - Fix: In 06-script-2-profiles.md and the `_scope.py` test list, replace `get_current_override_token()` with the documented API: `token = set_hermes_home_override(str(path))` returned and `reset_hermes_home_override(token)` on exit; `prev_token = set_hermes_home_override(_SENTINEL)` only if you really need the pre-state, then pass `prev_token` to reset. Add TDD tests: `test_scope_returns_token_from_set_hermes_home_override` (spy asserts set_hermes_home_override is called with the path, the returned token is what gets reset on exit) and `test_scope_does_not_leak_contextvar_across_nested_invocations` (assert that nested `hermes_home_scope` calls restore the OUTER override, not the post-inner state).

**[major]** 03-plugin-spec.md :: TDD test list - register_skill signature mismatch

  - Concern: The plan's `skill_register.py` sketch calls `ctx.register_skill(name='skill-creator', description=description, body=body, category=category)` (4 positional/keyword args). The real `PluginContext.register_skill` signature per `hermes_cli/plugins.py:1037-1042` is `register_skill(name: str, path: Path, description: str = "")` — it takes `name` and `path`, NOT `description`/`body`/`category`. Furthermore, the docstring at lines 1043-1049 explicitly states: "It does **not** enter the flat `~/.hermes/skills/` tree and is **not** listed in the system prompt's `<available_skills>` index — plugin skills are opt-in explicit loads only." This contradicts AC-1.4 ("the system-prompt index uses the bundled description") and AC-1.5 ("register via ctx.register_skill('skill-creator', ...)") and means the plugin's registered skill will NOT appear in the system prompt. The TDD test list (`test_register_calls_ctx_register_skill`) does not assert the actual public-API signature; it only asserts that `ctx.register_skill` is called with some kwargs. Coverage without the right assertion will pass even when the implementation uses an API that does not exist.
  - Fix: Rewrite AC-1.4 + 1.5: either (a) ship the skill into `~/.hermes/skills/<cat>/skill-creator/` directly via `installer.py` (i.e. NOT through `ctx.register_skill`) so it lands in the flat skills tree and the system prompt, or (b) accept that the plugin-bundled skill is opt-in only (`<plugin_name>:skill-creator` qualified name) and adjust AC-1.4 to reflect that reality. Either way, the TDD test `test_register_calls_ctx_register_skill` must assert the actual signature (name + path) and `test_register_loads_via_skill_view` must verify the skill is resolvable through the qualified-name path.

**[major]** 07-skill-creator-migration.md :: TDD test list - HERMES_SESSION nesting-guard claim

  - Concern: AC-4.6 names `HERMES_SESSION` as the canonical nesting-guard var, and Q1 is "escalated at Phase 4 HITL — see 12", with the escalation-log entry E1 marking `HERMES_SESSION` as confirmed. But my grep over `~/.hermes/hermes-agent/hermes_cli/*.py` and `agent/*.py` finds `HERMES_SESSION_ID`, `HERMES_SESSION_PLATFORM`, `HERMES_SESSION_CHAT_ID`, `HERMES_SESSION_THREAD_ID`, `HERMES_SESSION_CHAT_NAME`, `HERMES_SESSION_SOURCE`, `HERMES_SESSION_TOKEN` — no bare `HERMES_SESSION`. The plan's E1 escalation log says "confirmed" but there is no Hermes code path that reads `HERMES_SESSION` for nesting-guard purposes. The TDD test `test_hermes_subprocess_env_strips_guard` asserts the helper removes `HERMES_SESSION`; that will pass trivially since no real Hermes code reads the var. Coverage of the helper is fine, but the AC-4.6 claim that stripping `HERMES_SESSION` is what allows nested `hermes -p` is unsupported by the installed Hermes source. R5 risk in 12 acknowledges "if the var is undocumented, the default stands and the test asserts the helper un-nests a documented stub" — but the plan never demonstrates Hermes itself refuses nesting on this var. Without that, the test is testing the helper, not the contract.
  - Fix: Either (a) downgrade AC-4.6 from "Nesting-guard var: HERMES_SESSION" to "Strip an env var matching `HERMES_SESSION*` (glob) from the subprocess env to be safe; the precise var is operator-configurable via `NESTING_GUARD_VAR` in `_subprocess.py`" — and add a test that confirms `os.environ` after the run_eval still contains `HERMES_SESSION_ID` if set, and (b) add `test_real_hermes_cli_refuses_nested_invocation` (or document explicitly why it cannot run in CI) so the contract is anchored in real Hermes behavior, not just the helper. Also update Q1's escalation-log entry to reflect the truth: the var is not currently enforced by Hermes CLI; the helper is a forward-compatibility strip.

**[major]** 06-script-2-profiles.md :: TDD test list - no e2e "real hermes CLI" coverage for do_install signature

  - Concern: AC-3.6 pins `do_install(identifier, name_override="", force=True, skip_confirm=True, invalidate_cache=True)` as the exact signature to call. The plan claims this matches `hermes_cli/skills_hub.do_install`. Without an in-process signature assertion (e.g. `inspect.signature(do_install)`), the test will silently accept a wrong implementation. The TDD list has `test_apply_force_reinstall_on_version_drift` and `test_apply_calls_clear_skills_system_prompt_cache` but neither introspects the function signature. If the real signature differs (e.g. extra required args, different default), the implementation will TypeError at runtime — and there is no test that catches it.
  - Fix: Add `test_do_install_signature_matches_plan` — uses `inspect.signature(hermes_cli.skills_hub.do_install)` and asserts parameter names and defaults match AC-3.6 exactly. Mark this as `pytest.mark.skip` if the function is unavailable in the installed Hermes (e.g. version mismatch), but it must at least attempt the introspection so the AC is anchored to the real install.

**[major]** 04-script-1-patch.md :: TDD test list - line-drift anchored to a fixture checkout that doesn't exist in spec

  - Concern: `test_line_drift_exits_2_with_diagnostic` and `test_text_drift_exits_2_with_diagnostic` assert the script exits 2 on drift, but the plan never specifies how the drift is created in the fixture. It says "corrupt only the line content" and "anchor does not match" — but without a seed-minimal-fixture spec (see blocker #1) the test cannot reliably reproduce those drift conditions because the seed's anchor may itself have moved. Same issue for the `test_force_retries_only_drifted_sites` test in 04-script-1-patch.md and the `test_task_e_drift_exits_2` test in 05-script-1-task-e-toggle.md. The TDD list assumes a fixture whose bytes are pin-locked; the plan does not pin those bytes.
  - Fix: Add a `tests/fixtures/minimal_hermes/seed_minimal.py` module that lays down exact byte sequences for every anchor site (S1.cap, E1–E7), and reference this seed from the drift tests. Alternatively, add a `test_seed_minimal_anchors_match_patch_sites` to 09 that verifies the seed is in sync with the patch sites before any drift test runs.

**[minor]** 03-plugin-spec.md :: TDD test list - cap-state AST detection does not cover all 3 sites

  - Concern: The truncation research JSON enumerates three places where the 60-char cap is enforced: `agent/skill_utils.py:647-655` (authoritative), `hermes_cli/skills_hub.py:305` (hub catalog), `tools/skills_tool.py:1509` (operator TUI). The plugin's `_advisory.detect_cap_state` only inspects `agent/skill_utils.py` (line 85 in 03). If Hermes upgrades and the authoritative site moves (e.g., the constant is relocated to `hermes_constants.py`), the advisory will silently miss it. The TDD test `test_detect_cap_state_patched` and `test_detect_cap_state_unpatched` only exercise the one site. AC-4.10 requires "active-cap detection at install time" — same scope. No fallback site coverage.
  - Fix: Add `test_detect_cap_state_walks_all_known_sites` — fixture contains copies of the cap in `agent/skill_utils.py`, `hermes_cli/skills_hub.py`, AND `tools/skills_tool.py`; the detector returns the union state ("unpatched" if ANY site still has the literal 60). Document the fallback sites in 03 (or 12-R2) as something the detector scans.

**[minor]** 04-script-1-patch.md :: TDD test list - bilingual regex assertion has no negative cases for format violations

  - Concern: `test_console_log_lines_match_bilingual_regex` is listed in 04, 05, 07. But the AST-grep pre-commit hook sketch in 09 (`tools/check_bilingual.py`) only checks `print`/`logger.*` call literals. It does not catch: (a) `print(f"[en] {x} / [hu] {y}")` where x and y are runtime-computed, (b) `print("[en] " + msg + " / [hu] " + msg2)` — non-literal first arg, (c) bilingual content in docstrings/--help that is not a print call. The plan's "100% coverage" claim depends on the hook catching all violations; the test only asserts the positive path. No `test_bilingual_hook_catches_runtime_f_string` or `test_bilingual_hook_catches_string_concat` negative test is listed.
  - Fix: Add negative tests: `test_bilingual_hook_catches_runtime_f_string` (write `print(f"[en] {en_msg} / [hu] {hu_msg}")` to a tmp file, run the hook, expect exit 1); `test_bilingual_hook_catches_string_concat` (similar with `+`-concatenation); `test_bilingual_hook_warns_help_text_without_hu_marker`. Or restrict the hook's scope (only literal-string first args) and document the limitation in 09 with a note that runtime-formatted messages are checked by a separate snapshot test.

**[minor]** 06-script-2-profiles.md :: TDD test list - no concurrency / race test for hermes_home_scope + subprocess

  - Concern: The `hermes_home_scope` is reused by the installer, Script #2, and (per 11) the eval pipeline via the migrated `scripts/run_eval.py`. The plan's branch-coverage matrix in 09 says "every branch of the apply sequence" but the test list has no concurrency test for the case where two threads (or two concurrent profile iterators) enter `hermes_home_scope` simultaneously and one resets while the other is still running. The `ContextVar` semantics in `hermes_constants.py:16` (`_HERMES_HOME_OVERRIDE = ContextVar(...)`) provide isolation across asyncio tasks but NOT across threads; the plan never tests thread safety.
  - Fix: Either add `test_hermes_home_scope_thread_isolation_documented` that documents (via a comment-only assertion) the threading caveat, or add a `test_hermes_home_scope_serializes_per_profile` that proves the script processes profiles serially (which the design implies). Even better: add a meta-test that the per-profile apply loop runs sequentially (not concurrently) and document the limitation.

**[minor]** 09-test-strategy.md :: No-touch sentinel — real_hermes_agent_sentinel fails open if absent

  - Concern: `real_hermes_agent_sentinel` `pytest.skip`s if `~/.hermes/hermes-agent/agent/skill_utils.py` is missing. That means on a CI host that doesn't have Hermes installed, the no-touch guard is bypassed. A test that is supposed to PROVE that the worktree never touches `~/.hermes/hermes-agent` will silently skip — leaving the safety claim unverified. The TDD test `test_real_hermes_agent_sentinel_decorator_skips_when_absent` actually asserts this skip behavior, so the gap is structural, not a bug. But the safety claim "NEVER modify `~/.hermes/hermes-agent`" cannot be enforced by a test that itself skips on the very hosts that need it most.
  - Fix: Replace the skip-on-absent behavior with a hard fail (or a runnable fixture: lay down a tmp-path copy of the real `agent/skill_utils.py` before each test, hash that, and assert the real one was never touched AND the tmp_path copy is byte-identical to the real one at start). The current design conflates "Hermes is installed" with "we can verify we didn't touch it" — these are different conditions.

**[minor]** 07-skill-creator-migration.md :: TDD test list - T3.011 event-shape adapter test cannot anchor to real Hermes output

  - Concern: `test_event_shape_adapter_handles_known_shapes` parametrizes over "candidate Hermes event shapes" but the plan never lists the candidate shapes nor grounds them in real Hermes code. The Q2 escalation-log entry E2 says "re-verify shape at Phase 5 by reading `~/.hermes/hermes-agent/hermes_cli/streaming/`" — but at test-write time (Phase 5), the developer will need to enumerate the candidates from the Hermes source. Without a fixture of real Hermes NDJSON output (which the plan forbids running against `~/.hermes/hermes-agent`), the test only exercises guessed shapes. Coverage of the adapter is fine; coverage of the real Hermes event-shape contract is not.
  - Fix: Add to 07: a list of "candidate Hermes event shapes to support" derived from `hermes_cli/streaming/` (e.g. read the source during Phase 5 implementation and enumerate the shape variants). Or seed a `tests/fixtures/hermes_ndjson/*.jsonl` fixture file that is hand-captured from a Hermes session and committed, so the adapter test asserts byte-identity against real output. The latter is the only way to genuinely anchor the contract.

**[minor]** 05-script-1-task-e-toggle.md :: TDD test list - per-site anchor drift tolerance "+/- 2 lines"

  - Concern: `test_e1_anchor_at_line_172` says "fixture checkout; assert E1's current_text is at line 172 ± 2." But AC-2.2 (in 04) says "Each site is identified by BOTH a unique 8+ char anchor string AND a 1-based line number; mismatch on either → LINE_DRIFT (line) or TEXT_DRIFT (anchor). Both abort the run." The ±2-line tolerance contradicts the "exact line number" rule. Either AC-2.2 must be relaxed to "±2 lines" (with a note that Hermes minor-version drift is allowed), or the test_e1_anchor_at_line_172 must assert exactly line 172 (with no tolerance). The plan has both, contradicting each other.
  - Fix: Pick one: either (a) relax AC-2.2 to "anchor + ±2 lines", update 04-script-1-patch.md, and document this is intentional; or (b) tighten the test to assert exactly line 172, no tolerance. The current state means a passing test does not prove the script will reject drift on the same site in production (where the line really moved by 3).

**[nit]** 03-plugin-spec.md :: TDD test list - test_advisory_no_setattr_on_skill_utils is under-specified

  - Concern: `test_advisory_no_setattr_on_skill_utils` — assert the `_advisory` module does NOT import or setattr on `agent.skill_utils`. The test name suggests it checks `_advisory.py`, but AC-1.3 / Fix-ledger line 95 says "no setattr on `agent.skill_utils` anywhere". The test should be a meta-test that asserts the WHOLE `src/hermes_skill_creator_plugin/` tree has zero `setattr` calls referencing `agent.skill_utils` (or any submodule of `agent`). The current test scope is only one file.
  - Fix: Rename and broaden: `test_no_module_mutates_hermes_globally` — walks `src/hermes_skill_creator_plugin/` recursively, AST-parses every .py, asserts there are NO `ast.Call` nodes whose `func` is `setattr` AND whose first arg (as string) contains "agent.skill_utils" OR "agent.prompt_builder" OR any "hermes_*" module. Add this as a meta-test in 09-test-strategy.md.

**[nit]** 09-test-strategy.md :: Coverage matrix - script_2_profiles.py line-count target 400 vs budget 400

  - Concern: Per the coverage matrix, `scripts/script_2_profiles.py` has a target line-count of 400. Per 10-toolchain-and-conventions.md, every source file MUST be <= 500 lines (enforced by wemake's max-module-lines). 400 is within budget, but the TDD test list for Script #2 in 06 has 30+ tests spanning 50 lines; the implementation per the plan's per-profile apply sequence is ~80 lines + argparse + bilingual help + JSON report writer + frozen-time plumbing + hermes_home_scope interaction. Realistic estimate: 250-350 lines. The 400-line target is aspirational — if the implementation is closer to 350 the test coverage gate still holds. But the plan never validates that the tests' branch-coverage matrix (empty-profile / single-profile / N-profile / missing-home / set_hermes_home_override raise / do_install raise / clear_skills_system_prompt_cache raise matrix — 7 axes) actually achieves 100% on the predicted implementation. Branches not enumerated = branches not tested = coverage gap.
  - Fix: Add to 06: a branch-coverage matrix cross-reference to 09's existing matrix. For each branch in the implementation, list which test exercises it. Today 09 has the matrix but no traceability back to 06's specific code paths.

**[nit]** 03-plugin-spec.md :: TDD test list - test_install_idempotent_second_run_exits_0 sha256 contract under-specified

  - Concern: `test_install_idempotent_second_run_exits_0` says "second run with sha256 match; OK: already installed / OK: már telepítve". The test asserts a sha256 MATCH determines idempotency, but the installer's `install_plugin(target)` sketch does not show how it computes the sha256 (against what?). Does it hash the source `src/hermes_skill_creator_plugin/` directory? Does it hash a manifest of expected files? Does it compare against a previously-recorded sha256 in a sidecar file? The plan's installer sketch shows `install_plugin(target)` calls "sha256-based idempotency" without specifying the key. Test coverage of the matching branch is therefore undefined: a passing test does not prove the implementation matches the intended semantics.
  - Fix: Pin the sha256 contract in 03: e.g. "the installer records a `.install.sha256` sidecar next to the install root, containing sha256 of the source tree at install time. Subsequent runs compare current source-tree sha256 to the sidecar; mismatch → log 'collision detected' advisory, do not overwrite." Then `test_install_idempotent_second_run_exits_0` asserts: (a) the sidecar is written, (b) on second run with identical source, the file copies are NOT re-written (mtime unchanged), (c) bilingual log fires.

**[nit]** 04-script-1-patch.md :: TDD test list - .patch.state.json schema not asserted by any test

  - Concern: The `.patch.state.json` sidecar is referenced (AC-2.5 says `--force` reads it) but no test asserts its schema (e.g. `{site_id: "matched" | "drifted" | "patched"}`). `test_apply_creates_state_sidecar` asserts the sidecar exists with `{S1.cap: "patched"}` — but the schema for `"drifted"` and `"matched"` states is unstated. If the implementation serializes `"drifted"` as `"drift"` or `"LINE_DRIFT"`, the test passes anyway because it only exercises the `"patched"` branch.
  - Fix: Add `test_state_sidecar_schema_drifted` and `test_state_sidecar_schema_matched` — pre-write a sidecar with the wrong keys, run --apply, assert the implementation normalizes to the canonical enum. Or pin the schema explicitly in 04 and add a single `test_state_sidecar_schema_matches_spec` that walks every key.

**[nit]** 04-script-1-patch.md :: Coverage target - "100% line + branch" listed but no per-site coverage breakdown

  - Concern: "Coverage target" section in 04 says "100% line + branch coverage. Branches enumerated above. Every argparse choice exercised. Every exit code reachable by at least one test." Good. But the TDD test list enumerates 0..5 exit codes (0,1,2,3,4,5) and lists tests for each — yet there is no `test_exit_code_3_permission_denied` in the explicit list (only `test_target_unwritable_exits_3`, which uses chmod to simulate, and `test_apply_atomic_on_rename_failure`). Both should be present; `test_apply_atomic_on_rename_failure` exercises exit 4 (I/O) per AC-2.10, not exit 3. The branch-coverage matrix in 09 doesn't clarify which test owns which exit code.
  - Fix: Add a 1-to-1 exit-code x test-id table to 04 (and replicate the pattern in 06 for Script #2): explicit mapping. This catches mislabeled tests at review time.

**[nit]** 07-skill-creator-migration.md :: TDD test list - test_helper_is_single_source_of_truth relies on grep not on AST

  - Concern: `test_helper_is_single_source_of_truth` runs `grep -rE "HERMES_SESSION" src/hermes_skill_creator_plugin/` and asserts exactly one match. But the helper source file itself contains one match (the constant declaration). The docstring + the `if k != NESTING_GUARD_VAR` line and the helper body ALL reference the constant by name, so the actual match count will be 3+ (constant, body, docstring). The test as written will fail. This is a testability defect in the spec, not in the implementation.
  - Fix: Rewrite the test to be AST-aware: `test_helper_is_single_source_of_truth` — AST-walks every .py in `src/hermes_skill_creator_plugin/`, asserts there is exactly ONE assignment to a module-level binding matching the constant name pattern (`HERMES_SESSION*`), AND that all reads come from that one binding. Use `ast.walk` and check `ast.Name` and `ast.Constant` references. The grep-based assertion in the spec is broken.

**[nit]** 06-script-2-profiles.md :: Coverage target - no test for the no-op apply (everything already correct)

  - Concern: The TDD test list has `test_apply_idempotent_reinstall` (asserts do_install not re-called) and `test_audit_drift_detection` (asserts no diff when already correct). But there is no `test_apply_no_op_when_state_already_matches` — i.e. when `desired_state == current_state` AND `skill-creator` is already installed, `--apply` should write zero bytes (besides the JSON report). The branch-coverage matrix in 09 mentions "empty-profile / single-profile / N-profile / missing-~/.hermes matrix" but not the "state-already-matches" branch of the apply sequence. The early-exit branch of `--apply` is unstated and untested.
  - Fix: Add `test_apply_no_op_when_state_matches` — fixture with `openai` already disabled AND `skill-creator` already installed; assert `--apply` writes no profile-config changes, no hub install call, and emits a `NO_OP` action in the per-profile `actions_taken` array.

**[major]** 03-plugin-spec.md :: POSITIVE - V1 blockers correctly fixed in V2

  - Concern: POSITIVE FINDING (testability-and-coverage perspective): Compared to V1 (which had a `patch_runtime.py` and `extract_skill_description_patched.py` runtime monkey-patch path), V2 has correctly removed the runtime mutation and replaced it with static AST advisory + Script #1. The `test_hooks_does_not_setattr_on_skill_utils` (renamed `test_advisory_no_setattr_on_skill_utils` for the `_advisory` module) is present, and the plan claims it's broadened to "anywhere in `src/hermes_skill_creator_plugin/`" — this is the right shape. The `--target REQUIRED` safety gate is testable via `test_target_required_exits_4` and `test_target_resolves_to_hermes_agent_refused`. The active-cap detection (AC-4.10) is testable via `test_install_refuses_when_description_exceeds_active_cap` and `test_install_with_extended_description_uses_short_when_cap_unpatched`. These are all properly anchored to tmp_path fixtures, satisfying the "no touch of ~/.hermes/hermes-agent" constraint.
  - Fix: No fix needed; this is an explicit positive confirmation that V2 closed the V1 safety blockers per the testability lens.

**[major]** 09-test-strategy.md :: POSITIVE - tmp_path fixtures and conftest design is sound

  - Concern: POSITIVE FINDING: The `hermes_home` fixture (line 39-47) correctly uses `monkeypatch.setenv("HERMES_HOME", str(home))` to redirect ALL writes under `tmp_path/hermes-home/`. The `hermes_checkout` fixture (line 49-64) builds a USER-OWNED checkout under `tmp_path/hermes-checkout/` and never imports anything from `~/.hermes/hermes-agent`. The `real_hermes_agent_sentinel` fixture (line 71-85) hashes the live install before/after and fails if it changed — this is exactly the no-touch sentinel the testability-and-coverage lens demands. The TDD ordering in 09 (lines 176-191) puts `_scope.py` and `_subprocess.py` first because they have no Hermes dependencies, then escalates to installer/script/migrated — this is the correct sequence to maximize green-test velocity.
  - Fix: No fix needed; this is correct testability scaffolding.

---

### safety-and-non-execution — verdict: **request-changes**

- Blockers: 3
- Findings: 13

**[BLOCKER]** 04-script-1-patch.md :: Cap-raise site (the one site that ships in default mode) — site table row S1.cap

  - Concern: Plan anchors `S1.cap` at `agent/skill_utils.py:649`, but `truncation.json` (VERIFIED) shows the function `extract_skill_description` is defined at lines 647-655 with the truncation check `if len(desc) > 60:` at LINE 653, not 649. Line 649 is between the docstring and the `if not raw_desc:` block. On a clean v0.16.0 checkout (commit 368fcf1ff), every Script #1 run will emit LINE_DRIFT immediately, force every operator to use `--force --i-accept-line-drift`, and the audit log will fill with one-time-warning entries. This is both a safety concern (the safety guard forces operators into the line-drift override path) and a correctness concern (multi-signal targeting AC-2.2 is not satisfied at the canonical site).
  - Fix: Re-derive the line anchor by reading `/Users/kiscsicska/.hermes/hermes-agent/agent/skill_utils.py` at the planning phase (per the brief: 'Truncation site: agent/skill_utils.py lines 644-655'). Update `S1.cap` to `agent/skill_utils.py:653` (the exact line of the `if len(desc) > 60:` check) OR specify an anchor that is unique at line 649. The same fix is required for the `.patch.rejected` report example (currently shows `actual_at_line_649`). Add a `validate_anchors.py` Phase 5 test that re-reads the live source and asserts the cited lines still contain the expected text.

**[BLOCKER]** 03-plugin-spec.md :: plugin.json (manifest) — `kind=skill_authoring` field

  - Concern: `pluginAuthoring.json` (read-only-verified) documents the only known `kind` value as `backend` (for sub-plugins under `plugins/web/`, `plugins/browser/`, etc.). The plan declares `kind=skill_authoring`, which is NOT in the documented set. AC-1.5 says the plugin "passes hermes-agent-skill-authoring frontmatter validation" — but the plugin.json manifest is parsed by `PluginManager._parse_manifest`, not the skill validator. An unknown `kind` is likely to (a) be silently ignored, leaving the plugin loaded under the wrong lifecycle, or (b) be rejected at install time. Either outcome is a safety hazard because the user assumes the cap-raise is handled by the plugin when the plugin is in a half-loaded state. The brief pinned `hermes-agent-skill-authoring` for the SKILL.md frontmatter, not the plugin manifest.
  - Fix: Either (a) remove `kind=skill_authoring` from the plugin.json (use no kind at all — the default path), or (b) confirm the literal string `skill_authoring` against the live `hermes_cli/plugins.py` parser at Phase 5 / B-plugin. Add a Phase 5 test that parses the manifest with the real `_parse_manifest` and asserts it loads under the intended discovery root (`~/.hermes/plugins/hermes-skill-creator-plugin/`). Per `pluginAuthoring.json` evidence, the only verified kind is `backend`, and this plugin is NOT a backend — it's a hook+skill plugin.

**[BLOCKER]** 07-skill-creator-migration.md :: T3 inventory (per-binding replacement table)

  - Concern: AC-4.5 says "Every Claude-specific invocation replaced per the T3 inventory (15 rows in 07)", but the T3 inventory in plan 07 enumerates 15 rows, none of which are in `scripts/run_loop.py`. `anthropicCreator.json` (read-only-verified) lists `scripts/run_loop.py:252` (the `--model` flag bound to `claude` model ids) and `scripts/run_loop.py:1` (module docstring) as Claude-specific bindings. `scripts/run_loop.py` is the orchestrator for the description-optimization loop and is the second-highest-risk Claude-specific surface after `improve_description.py` and `run_eval.py`. The migration fidelity claim in the strength-preservation matrix and the AC-4.5 audit gate are both violated. The plan's tool-name mapping table does not even include a row for `run_loop.py`.
  - Fix: Add T3 rows 16, 17, 18 for `scripts/run_loop.py` (the orchestrator): T3.016 = `scripts/run_loop.py:252` (--model flag rename to `hermes-...`); T3.017 = `scripts/run_loop.py:1` (module docstring 'Run description optimization loop for a Hermes skill'); T3.018 = `scripts/run_loop.py` body (any other Claude CLI invocations that may surface). Update MIGRATION.skill-port.md row count to 18 (was 15). Update AC-5.5 to require 18 rows in the T3 inventory. Add the migrated `scripts/run_loop.py` to file 07 §TDD test list with per-binding tests.

**[major]** 04-script-1-patch.md :: Cap-raise site — replacement_text and the `from tools.skills_tool import MAX_DESCRIPTION_LENGTH` import

  - Concern: The patch inserts `from tools.skills_tool import MAX_DESCRIPTION_LENGTH` at the top of `agent/skill_utils.py`. `truncation.json` evidence shows `agent/skill_utils.py` is already imported BY `tools/skills_tool.py:482-489` (`_parse_frontmatter re-export of agent.skill_utils.parse_frontmatter`) — a `tools.skills_tool` → `agent.skill_utils` import would create an import cycle on first load. Plan never verifies the import direction is acyclic. If the cycle fires, `agent/skill_utils.py:extract_skill_description` fails to import, Hermes's prompt builder crashes at session start, and the cap-raise is now a hard outage instead of an improvement.
  - Fix: Use a local constant in `agent/skill_utils.py` (e.g. `_DESCRIPTION_CAP = MAX_DESCRIPTION_LENGTH` resolved at module load via `from tools.skills_tool import MAX_DESCRIPTION_LENGTH` placed AFTER `tools.skills_tool`'s full module load — actually impossible to guarantee) OR duplicate the constant literally in skill_utils.py (e.g. `MAX_DESCRIPTION_LENGTH = 1024` defined in skill_utils.py too, with a comment that says "kept in sync with tools/skills_tool.py:95"). The safer pattern: replace `if len(desc) > 60:` with `if len(desc) > 1024:` (a literal constant), since both 1024 and the matching `+ "..."` pattern are already proven in tools/skills_tool.py:655-656. Add a Phase 5 test that imports `agent.skill_utils` after the patch and asserts no `ImportError` is raised.

**[major]** 06-script-2-profiles.md :: Per-profile apply sequence — hermes_home_scope + do_install env mirror

  - Concern: Plan 02-architecture §3 and 06 §hermes_home_scope say the context manager must mirror BOTH `set_hermes_home_override(path)` AND `os.environ['HERMES_HOME'] = str(path)`, citing "D7 dispute" between override-token readers and env-var readers. `profileSystem.json` evidence says `hermes_cli.skills_hub.do_install(identifier, ...)` uses `get_hermes_home()` (override token), not the env var. The env-var mirror in hermes_home_scope is therefore NOT load-bearing for `do_install`, but the plan keeps it because the dispute is "unresolved." The plan's try/finally restore on `os.environ['HERMES_HOME']` is a NEW code path the operator does not need; if the operator's shell already had HERMES_HOME set, the script's restore will be silent. If the operator did NOT have HERMES_HOME set, the script's `os.environ.pop('HERMES_HOME', None)` on exit is benign. The real risk is that the env-var mirror is unnecessary ballast that adds a coverage branch with no test value.
  - Fix: Re-derive D7 at Phase 5 / D-script-2: read the actual `hermes_cli/skills_hub.py:478` `do_install` body and confirm whether it reads `os.environ['HERMES_HOME']` anywhere. If not (which is the most likely outcome per profileSystem.json evidence), DROP the env-var mirror from `hermes_home_scope` and pin the override token as the single source of truth. The safety claim in plan 02-architecture §Safety recap should be tightened: "All file writes (installer + Script #2) go through `hermes_home_scope` which sets and restores the override token" — drop the `os.environ['HERMES_HOME']` clause. This is a CORRECTNESS finding, not a safety blocker, but it does affect the 100% coverage claim on the dual-mirror.

**[major]** 04-script-1-patch.md :: CLI surface — `~/.hermes/patch-audit.log` write

  - Concern: AC-2.5.1 says `--force` "appends to `~/.hermes/patch-audit.log` with timestamp + diff hash" but does NOT route this write through `hermes_home_scope` or require a `--yes` / TTY confirmation. The audit log lives in the operator's home directory (`~/.hermes/`, NOT `~/.hermes/hermes-agent/`) and is a NEW file the plan creates. Plan 02 §Safety recap does not mention this write. The plugin installer has a `--yes` / TTY gate; the audit-log write does not. If the operator's `~/.hermes/` is read-only (e.g., a frozen Nix store or a CI sandbox), the write fails and Script #1 exits non-zero on an audit write, even when the patch itself succeeded.
  - Fix: Either (a) move the audit log to a path the operator can suppress (`HERMES_PATCH_AUDIT_LOG=/dev/null` to silence; default `~/.hermes/patch-audit.log`), or (b) make the audit-log write best-effort: try the write inside a try/except and log a bilingual warning if it fails, but exit 0 on patch success. Document in plan 04 §Safety gates: "audit-log write is best-effort; failure to write the log does NOT fail the patch." Add a test that pre-creates a read-only `~/.hermes/` and asserts --force still exits 0.

**[major]** 09-test-strategy.md :: No-touch sentinel for `~/.hermes/hermes-agent` — real_hermes_agent_sentinel fixture

  - Concern: Plan 09 §Fixture strategy defines a `real_hermes_agent_sentinel` fixture that hashes `~/.hermes/hermes-agent/agent/skill_utils.py` at test start and asserts the hash is unchanged at test end. The fixture SKIPS the test if the file is absent. This is a SAFETY hazard: a test that runs in CI on a Hermes-developer host (the most common case for this worktree) will read a live file from the read-only install at every test invocation. The plan's "NEVER read `~/.hermes/hermes-agent`" rule (from the brief and plan 00 §Hard constraints) is contradicted by this fixture. The plan's argument is "reading is allowed; only writing is forbidden," but the fixture's behavior of fingerprinting a live file is brittle (Hermes's auto-update or background process can change the file mid-test) and the assertion `pre == post` would fail spuriously.
  - Fix: Replace the live-sentinel with a fully synthetic sentinel: at test setup, copy `/Users/kiscsicska/.hermes/hermes-agent/agent/skill_utils.py` to `tmp_path/hermes-fake-install/agent/skill_utils.py` ONCE per pytest session (using a session-scoped fixture that reads the file ONCE); run the actual scripts against `tmp_path/hermes-fake-install`; assert the SHA of `tmp_path/hermes-fake-install/agent/skill_utils.py` is unchanged. The live file is read at most ONCE per session, not per test. State this explicitly in the fixture docstring. Per the V1 review's [major] finding on this exact topic, the fix was not landed.

**[major]** 03-plugin-spec.md :: Plugin layout — `~/.hermes/plugins/<name>/` install destination and the bundled skill path

  - Concern: Plan 03 §Plugin layout shows the plugin layout is `src/hermes_skill_creator_plugin/skills/skill-creator/SKILL.md` (bundled under the plugin package) and the install destination is `~/.hermes/plugins/hermes-skill-creator-plugin/`. `pluginAuthoring.json` (verified) confirms `~/.hermes/plugins/<name>/` is the user install root. However, the plan's `register_skill` uses `ctx.register_skill('skill-creator', ...)` and the `pluginAuthoring.json` evidence says `ctx.register_skill(name, path, description='')` registers the skill as NAMESPACED (`<plugin>:<name>`) and NOT in the flat global skills index — meaning the system prompt's `<available_skills>` block will NOT include the migrated skill, defeating the "re-publishes" claim. AC-1.4 says the plugin "exposes a `register` entry point that calls `ctx.register_skill('skill-creator', ...)` with the BUNDLED frontmatter" — but the discovered-skills list the LLM sees will not include it. The user must ALSO install the skill to `~/.hermes/skills/<cat>/skill-creator/` for the LLM to see it. This contradicts the brief's "no additional install step" claim and is a SAFETY-adjacent hazard (the user may think the plugin is sufficient and skip Script #2).
  - Fix: Clarify AC-1.4 in plan 01: "the plugin's `register` entry point calls `ctx.register_skill('skill-creator', ...)` AND the plugin installer (or Script #2) writes a flat-namespace copy to `~/.hermes/skills/<cat>/skill-creator/SKILL.md` so the LLM's available-skills index includes it." Add a Phase 5 test `test_skill_appears_in_available_skills_index` that walks the hermes home's `~/.hermes/skills/` tree after Script #2 --apply and asserts the migrated skill's SKILL.md is present. Per V1 review's [minor] finding on this exact issue, the fix landed as a docstring clarification in plan 03 but the AC was not updated.

**[minor]** 04-script-1-patch.md :: CLI surface — `--emit-migration-note` writes to worktree root

  - Concern: Plan 04 §CLI surface and 02-architecture §2 say `--emit-migration-note` "regenerates MIGRATION.hermes-patch.md and MIGRATION.md index in the WORKTREE (NOT the target)." The plan never pins WHICH worktree. If the operator runs the script from `/Users/kiscsicska/projects/easter-skills-hack-to-hermes-2/.claude/worktrees/hermes-skills-dev/` (the development worktree, per plan 00), the files land there. If the operator runs from a Phase 5 worktree (per plan 11 §Workstream table: `phase5/C-script-1`), the files land in that worktree. If the operator's `cwd` is somewhere else, the script will fail (no writable worktree) or write to the wrong location. Plan needs to pin: MIGRATION files always land in the worktree that contains the `plans/` directory and the `pyproject.toml` (the canonical worktree root), regardless of `cwd`.
  - Fix: Add to plan 04 §CLI surface: "`--emit-migration-note` writes `MIGRATION.md` and `MIGRATION.hermes-patch.md` to the worktree root resolved by walking UP from `cwd` to find the first directory containing `pyproject.toml` and `plans/` (the canonical worktree root). If no such directory is found, the script exits 4 with a bilingual error." Add a Phase 5 test that runs `--emit-migration-note` from a `tmp_path/subdir/` cwd and asserts the files land at `tmp_path/` (the resolved worktree root), not at `tmp_path/subdir/`.

**[minor]** 04-script-1-patch.md :: Cap-raise site — MAX_DESCRIPTION_LENGTH duplication

  - Concern: `truncation.json` and `hermesSkillConventions.json` show `MAX_DESCRIPTION_LENGTH = 1024` is defined in TWO places: `tools/skills_tool.py:95` and `tools/skill_manager_tool.py:112`. Plan 04 only patches `agent/skill_utils.py:649` (the consumer). The producers (the two constant definitions) are left untouched. If Hermes's prompt builder or skills_tool iterates and reads the constant from the producer files, the cap-raise is half-done. The plan acknowledges the constant exists at both producer sites but does not commit to patching them. Per `truncation.json` evidence, the unpatched-consumer-vs-patched-producer configuration can produce inconsistent behavior: the system-prompt index would use 1024 (patched skill_utils), but the skills_list / skill_view tools (using tools/skills_tool.py) would use 1024 (already), and the validator (using tools/skill_manager_tool.py) would use 1024 (already). The patch is therefore a no-op for the 1024 sites, which is fine; the plan should DOCUMENT this so a future maintainer does not "complete" the patch and break things.
  - Fix: Add a one-line note to plan 04 §Cap-raise site: "The `MAX_DESCRIPTION_LENGTH` constant is defined in `tools/skills_tool.py:95` and `tools/skill_manager_tool.py:112` (both already =1024). This patch only touches the consumer at `agent/skill_utils.py:649`; the producer constants are intentionally left at 1024 (the target value) to avoid a 3-way constant divergence." This is a documentation gap, not a safety gap, but it affects the AC-2.1 idempotency claim — re-running --check on a fully-patched install should detect that the producer constants are already at 1024 and the consumer is now at 1024 and exit 0. The current `S1.cap` site table does not cover the producer sites.

**[minor]** 07-skill-creator-migration.md :: hermes_subprocess_env() helper — single source of truth for HERMES_SESSION

  - Concern: Plan 07 §hermes_subprocess_env helper pins `NESTING_GUARD_VAR = "HERMES_SESSION"` and the helper strips it from the subprocess env. `anthropicCreator.json` evidence says the nesting-guard var name is "TBD exact name - likely HERMES_SESSION or HERMES_AGENT; must be confirmed in §5.5 migration note." Plan 12 §Escalation log E1 confirms HERMES_SESSION as the chosen default but the research never confirmed it against the live Hermes source. The plan pins HERMES_SESSION as "single source of truth" — meaning if HERMES_SESSION is NOT the actual var, every subprocess call will still nest-block, and the eval pipeline will fail silently. The plan defers confirmation to Phase 5 implementation, but the helper is the load-bearing piece.
  - Fix: Add a Phase 5 pre-implementation test `test_hermes_nesting_guard_var_confirmed` that reads `~/.hermes/hermes-agent/agent/` and `~/.hermes/hermes-agent/hermes_cli/` for any `os.environ.get("HERMES_*")` reference and asserts at least one of the var names (HERMES_SESSION, HERMES_AGENT, HERMES_PARENT) is referenced. If NONE is found, the test skips and the helper is updated to a NO-OP (does not strip any var). The plan's Q1 → HERMES_SESSION decision (E1) should be promoted from "confirmed at HITL" to "confirmed at Phase 5 by reading the live Hermes source."

**[minor]** 06-script-2-profiles.md :: Desired state per profile — `openai` always disabled

  - Concern: Plan 06 §Desired state pins `openai` as ALWAYS disabled, with the rationale "it's the upstream source of the original Anthropic skill-creator." But `hermesSkillConventions.json` does NOT list `openai` as a Hermes tool name (only `delegate_task`, `mixture_of_agents`, etc.). `profileSystem.json` does not mention `openai` either. The `openai` string is most likely an OpenAI-CLI integration (e.g., an external tool provider registered as "openai") — not a Hermes skill. Disabling it via `save_disabled_skills(config, names=["openai"])` is correct only if the audit first detects an "openai" entry in the disabled-skill set. If `openai` is not present in the set, `save_disabled_skills` may add a no-op entry, polluting the config. The plan should gate the disable on detection, not unconditionally add.
  - Fix: Update plan 06 §Desired state: "`openai` is added to the desired disabled set iff the current installed-skill list (or the global plugin list) contains an `openai` artifact." Add a Phase 5 test that runs Script #2 on a fixture profile with no `openai` skill present, asserts `desired_disabled` does NOT include `"openai"`, and asserts `current_disabled` is unchanged. The current `test_audit_skills_global_absent` covers the `skills` case but not the `openai` case.

**[nit]** 12-risks-and-open-questions.md :: Residual risks — R6 (clear_skills_system_prompt_cache fallback)

  - Concern: Plan 12 §R6 documents the fallback "if `clear_skills_system_prompt_cache` does not exist in v0.16.0, 06 falls back to deleting `~/.hermes/.skills_prompt_snapshot.json` directly." `agent/prompt_builder.py` evidence (per the taskECandidates.json's "reconciledFinalSet" referencing agent/prompt_builder.py) confirms the function exists at the canonical path. The fallback is therefore unreachable in v0.16.0. Adding the fallback code path adds a branch that is never exercised in production and inflates the 100% coverage requirement. The plan should pin the function's existence at Phase 5 and remove the fallback if the function is confirmed present.
  - Fix: Add a Phase 5 pre-implementation test `test_clear_skills_system_prompt_cache_exists` that does `from agent.prompt_builder import clear_skills_system_prompt_cache; assert callable(clear_skills_system_prompt_cache)`. If it passes, drop the fallback from plan 06 §Per-profile apply sequence. If it fails, the fallback stays. This is a coverage-burden reduction, not a safety concern.

**[nit]** 05-script-1-task-e-toggle.md :: Out of scope (explicit) — system_prompt.py is NOT a patch site

  - Concern: Plan 05 §Out of scope says "`system_prompt.py` is NOT a patch site. It imports `MEMORY_GUIDANCE` and `SKILLS_GUIDANCE` from `prompt_builder` (per `agent/system_prompt.py:34, 38`) and is a CONSUMER, not a source. Patching it would be a no-op once `prompt_builder` is patched." This is correct, but the taskECandidates.json research's `reconciledFinalSet` (sixth entry) does propose a comment-only edit at `agent/system_prompt.py:~117` (the `tool_guidance.append` assembly site) to pin the synchronization contract. Plan 05 explicitly omits this site, but the research supports it as a docstring-only edit. This is a docstring-consistency nit, not a safety concern.
  - Fix: Either (a) drop the "system_prompt.py is NOT a patch site" claim from plan 05 and replace with "system_prompt.py gets a comment-only edit at the tool_guidance assembly site to pin the sync contract" (a no-behavioral-change edit), or (b) explicitly defer the system_prompt.py comment to a separate "documentation hygiene" workstream in plan 11. The current state (silently omitting it) leaves a small drift hazard.

**[nit]** 01-overview.md :: Fix ledger — AC-1.3 "no setattr on agent.skill_utils" — test enforcement

  - Concern: Plan 01 fix ledger claims AC-1.3 is tightened: "no setattr on `agent.skill_utils` anywhere; enforced by import-graph test." Plan 03 §TDD test list has `test_hooks_does_not_setattr_on_skill_utils` ("assert the hooks module has NO import-time mutation of `agent.skill_utils`"). The test is named "import-time mutation" — but a `setattr` inside a function body is not an import-time mutation. The test should enumerate BOTH the import-time check (no `import agent.skill_utils; agent.skill_utils.X = ...` at module load) AND the runtime check (no `setattr(skill_utils, ...)` inside any function). Currently the test name is ambiguous.
  - Fix: Split `test_hooks_does_not_setattr_on_skill_utils` into two tests: `test_hooks_does_not_mutate_skill_utils_at_import` (AST-grep the hooks module for top-level `agent.skill_utils.* = ` assignments) and `test_hooks_does_not_setattr_skill_utils_at_runtime` (AST-grep the hooks module for `setattr(agent.skill_utils, ...)` or `setattr(skill_utils, ...)` calls in function bodies). Both tests must pass for AC-1.3 to be considered enforced.

**[nit]** 02-architecture.md :: Data flow — Plugin install path step 5 "active-cap guard"

  - Concern: Plan 02-architecture §1 step 5 says the installer "Validates the migrated skill's description against the active cap; aborts with bilingual error if it exceeds." Plan 03 §installer (the code sketch) does the same check, but uses a magic-number 1024 vs 60 (`active_cap = 1024 if cap_state == "patched" else 60`) and the --with-extended-description flag substitutes the SKILL.md.short variant. The bilingual error message references "active cap 60" but the actual cap source is the static-AST read. A safer pattern is to derive the cap from the static-AST read result, not from a hardcoded `1024 vs 60` lookup, because Hermes could in principle change the unpatched value (e.g., to 100 for a different design reason).
  - Fix: Update plan 03 §installer sketch: `active_cap = MAX_DESCRIPTION_LENGTH if cap_state == "patched" else 60` — read the cap value from the AST scan, not from a literal `1024`. The static-AST scan already returns `"patched"` or `"unpatched"`; extend it to also return the literal cap value (e.g., `detect_cap_state(target) -> (str, int)` returning (state, cap_value)). Add a Phase 5 test `test_active_cap_derived_from_ast_not_hardcoded` that pre-populates a target with a custom cap (e.g., `if len(desc) > 100:`) and asserts the installer uses 100, not 60.

---

### migration-fidelity — verdict: **request-changes**

- Blockers: 3
- Findings: 16

**[BLOCKER]** 07-skill-creator-migration.md :: T3 inventory (table of 15 rows)

  - Concern: Multiple T3 inventory line numbers in the plan do not match the pinned vendored source at research/anthropic-skill-creator-original/skills/skill-creator/. Verified by grep: (a) T3.007 says SKILL.md:432 is a "claude.ai URL reference" — actual line 432 is the prose "*Description optimization*: This section requires the `claude` CLI tool (specifically `claude -p`) which is only available in Claude Code. Skip it if you're on Claude.ai." — no URL is on that line. (b) T3.008 says SKILL.md:445-455 is the "Cowork-specific section" — actual line 445 is the heading "## Cowork-Specific Instructions" and 455 ends with "Follow the update guidance in the claude.ai section above." (not pure Cowork). (c) T3.009 says SKILL.md:483 is the "Cowork fallback line" — actual line 483 is "Please add steps to your TodoList, if you have such a thing, to make sure you don't forget. If you're in Cowork, please specifically put 'Create evals JSON and run `eval-viewer/generate_review.py` so human can review test cases' in your TodoList" (it's a TodoList/Cowork sentence, not a single Cowork fallback line). (d) T3.010 says SKILL.md body has "if not webbrowser.open(...) (Cowork browser auto-open)" — but the actual reference at line 247 is "**Cowork / headless environments:** If `webbrowser.open()` is not available or the environment has no display, use `--static <output_path>`..." — there is no Python `if not webbrowser.open(...)` codepath in SKILL.md (it's a Markdown instruction paragraph). (e) T3.003 says `scripts/run_eval.py:71-87` is the `claude -p --output-format stream-json --verbose --include-partial-messages` block — actual block is at lines 70-76; the comment and env-strip are at lines 80-83. These off-by-N errors mean the migrated-files tests in TDD will not find the binding string at the cited line; the migration-fidelity invariant "every Claude-binding has a passing test" is unprovable against the real vendored source.
  - Fix: Re-derive every T3 row line number from research/anthropic-skill-creator-original/skills/skill-creator/ (read-only). For T3.007, use the line that actually contains "claude.ai" as a reference (line 455: "Follow the update guidance in the claude.ai section above."). For T3.009, pick a single Cowork-fallback line or split into two rows. For T3.010, replace with a row that targets line 247's `webbrowser.open()` prose and a separate row targeting the Python codepath (the only Python `webbrowser.open` reference is in eval-viewer/generate_review.py, not in SKILL.md). For T3.003, narrow the row to lines 70-76 (cmd list) and add a separate T3 row for the env-strip at lines 80-83. Add a `test_t3_anchors_match_vendored_source` that asserts every cited `path:line` in the T3 table resolves to the cited binding string in the read-only vendored copy. This is the load-bearing migration-fidelity test.

**[BLOCKER]** 07-skill-creator-migration.md :: Tool-name mapping table (lines 41-60) vs hermesSkillConventions.json

  - Concern: The plan's tool-name mapping table is INCOMPLETE vs the authoritative list in plans/_research/hermesSkillConventions.json (allowedAndForbiddenInvocations). Verified discrepancies: (a) Plan maps `Bash → terminal` only, but hermesSkillConventions.json lists THREE Bash analogues (`terminal` for state mutations, `read_terminal` for read-only inspection, `execute_code` for sandboxed Python). The migrated scripts/run_eval.py uses `subprocess.run(["hermes", ...])` which is Bash semantics; the table must enumerate all three so a reviewer can pick the right one per call site. (b) Plan maps `Read → read_file`, but the JSON also notes that for linked-file subresources Hermes uses `skill_view(name, file_path=...)` (a second Read analogue). (c) Plan maps `TodoWrite → todo` ✓; but the JSON additionally lists `process` (process management), `image_generate`, `video_generate`, `tts`, `vision_analyze`, `kanban_*`, `session_search`, `send_message`, `computer_use` — none of which the plan enumerates. The plan's table is presented as exhaustive but is not. (d) The plan does NOT list the FORBIDDEN Claude-Code-only tools (`EnterPlanMode`/`ExitPlanMode`) under the migration table — the JSON explicitly states these are forbidden and the replacement is `delegate_task` + prose, but the plan's table has no row for them. (e) `CronCreate` / `CronDelete` / `CronList` mapping in the plan is `cronjob (action=add/del/list)` — correct, but the JSON also requires this to be reflected in `tool_name.lower() in (...)` matches; the plan's AC 4.7 (lines 67 of 07) only says lowercase matching without enumerating the full set. Migration-fidelity is undermined because the migrated SKILL.md body cannot be statically proven to use only the listed names — a single `terminal` instead of `execute_code` for a Python heredoc, for example, will be undetectable.
  - Fix: Expand 07's tool-name mapping table to mirror the full list from hermesSkillConventions.json. Add rows for `Bash → terminal | read_terminal | execute_code` (with selection rules: state mutations → terminal, read-only inspection → read_terminal, sandboxed Python → execute_code); add `EnterPlanMode/ExitPlanMode → FORBIDDEN, use delegate_task + prose`; add a "FORBIDDEN Claude-Code-only tools" row listing the PascalCase names; cite the JSON file as the source-of-truth in the table caption. Update AC 4.7 to require the migrated SKILL.md body to contain no tool name outside the full hermesSkillConventions.json list. Add `test_tool_name_table_is_superset_of_research_json` that diff-checks the plan's table vs the JSON.

**[BLOCKER]** 07-skill-creator-migration.md :: hermes_subprocess_env helper (lines 86-101) vs anthropicCreator.json (lines 268-286)

  - Concern: The plan's hermes_subprocess_env() helper strips ONLY the HERMES_SESSION var (NESTING_GUARD_VAR = "HERMES_SESSION"). Verified vs the authoritative research at anthropicCreator.json (proposedHermesForm for `scripts/improve_description.py:33`): the correct migration strips BOTH `CLAUDECODE` AND the Hermes nesting-guard var (`{k: v for k, v in os.environ.items() if k not in {"CLAUDECODE", "HERMES_SESSION"}}`). The plan drops the CLAUDECODE strip, which is a fidelity bug: if a Hermes operator runs the migrated `run_eval.py` from inside a Claude Code session (a legitimate back-compat scenario, since Anthropic and Hermes may coexist on the same dev machine), the inner `hermes -p` subprocess will see CLAUDECODE in its env and either refuse to nest or behave undefined. The research JSON is explicit; the plan diverges.
  - Fix: Update hermes_subprocess_env() in 07 to strip BOTH `CLAUDECODE` (legacy) AND `HERMES_SESSION` (current). The helper should be: `return {k: v for k, v in os.environ.items() if k not in {NESTING_GUARD_VAR, "CLAUDECODE"}}`. Update AC 4.6 to require both. Add a test `test_hermes_subprocess_env_strips_both_guards` that sets both `CLAUDECODE=1` and `HERMES_SESSION=foo` and asserts neither appears in the returned dict. Update the T3 inventory T3.002 and T3.004 rows to reflect "strips CLAUDECODE + HERMES_SESSION" rather than just the Hermes guard.

**[major]** 07-skill-creator-migration.md :: Frontmatter proposal (lines 13-30)

  - Concern: Plan omits the Hermes-specific allowed frontmatter keys enumerated in hermesSkillConventions.json: `model`, `when_to_use`, `platforms`, `environments`, `prerequisites.{env_vars,commands}`, `required_environment_variables`, `required_credential_files`, `setup.{help,collect_secrets}`, `metadata.hermes.{tags,related_skills,requires_toolsets,fallback_for_toolsets,requires_tools,fallback_for_tools,config}`. Plan's frontmatter (lines 13-30) only declares `name`, `description`, `version`, `author`, `license`, `metadata.hermes.tags`, `metadata.hermes.related_skills`. The validator (skill_manager_tool.py:_validate_frontmatter) is a permissive allowlist (everything not in the rejection list is allowed), so the frontmatter will pass — but the plan does not document WHY the missing keys are absent (e.g., "model omitted because the skill should run under whatever model the user has configured; pinning would break the run-eval's `--model hermes-...` flow"). Migration-fidelity requires the plan to enumerate the chosen key set vs the validator's allowed set.
  - Fix: Add a paragraph to 07 §Frontmatter explicitly listing the omitted keys and the reason for omission (e.g., "model omitted: skill is model-agnostic per hermes-agent-skill-authoring SKILL.md line 144"; "when_to_use omitted: description starts with 'Use when' which is the peer convention"; "platforms/environments omitted: skill is cross-platform"). Add AC 4.2.1: "frontmatter passes `_validate_frontmatter` from tools/skill_manager_tool.py with all Hermes-allowed keys documented in a per-key justification comment in the SKILL.md source." Update `test_frontmatter_passes_hermes_validator` to assert the resulting list of keys is a subset of the validator's allowlist.

**[major]** 07-skill-creator-migration.md :: Strength preservation matrix (lines 119-123)

  - Concern: Plan asserts "subagent split preserved" with the replacement "agent_name registration in Hermes's delegate_task". Verified vs pluginAuthoring.json: Hermes's subagent dispatch is via `ctx.register_hook`, `ctx.register_auxiliary_task`, `delegate_task` (tools/delegate_tool.py), and `mixture_of_agents` (tools/mixture_of_agents_tool.py) — NOT a YAML-frontmatter `agent_name` field. There is no `agent_name` field in any Hermes subagent registration. The three Anthropic agents (grader, analyzer, comparator) are defined in the vendored source as Anthropic's "Task tool subagent" pattern with YAML frontmatter for description/tools/prompt; Hermes has no equivalent on-disk file format. The plan's "agent_name registration" is invented. Migration-fidelity fails: a reviewer cannot verify the three subagents are wired correctly because the wiring shape is not defined.
  - Fix: Replace the strength-preservation matrix's subagent row with the actual Hermes mechanism. Option (a): each agent becomes an aux-task registered via `ctx.register_auxiliary_task(name="grader", description=..., defaults=...)`; the agent's "prompt" becomes a SKILL.md under skills/<category>/<agent-name>/SKILL.md and the task dispatches a Hermes subagent that loads that skill. Option (b): keep the three files as standalone SKILL.md assets under scripts/skill-creator/agents/ and have the parent skill dispatch them via `delegate_task(agent_name="grader", context={...})`. Pick one, document the call shape, and add an integration test that spawns a subagent with each role and asserts the result shape matches the Anthropic grader/analyzer/comparator output schema. The "agent_name registration" framing must be deleted.

**[major]** 04-script-1-patch.md :: Cap-raise site (line 15) — `agent/skill_utils.py:649`

  - Concern: Plan cites the cap-raise line as `agent/skill_utils.py:649` with anchor `if len(desc) > 60:`. Verified at /Users/kiscsicska/.hermes/hermes-agent/agent/skill_utils.py: the literal `if len(desc) > 60:` is at line 653, not 649. The function def is around line 647. Multi-signal targeting (AC-2.2) requires the line number to be exact; a 4-line drift in a real Hermes checkout will cause the `LINE_DRIFT` diagnostic to fire on the first `--check` run and require the operator to use `--force --i-accept-line-drift` immediately. This is a real Hermes install — the drift is observable.
  - Fix: Update 04 §Cap-raise site to `agent/skill_utils.py:653` (the actual `if len(desc) > 60:` line). Update `MIGRATION.hermes-patch.md` cap-raise row accordingly. Add a `test_anchors_match_hermes_v0_16_0` integration test that runs against the real Hermes install (read-only) and asserts each cited line in 04 and 05 contains the cited anchor string. This test will run only in CI when the Hermes source is available; locally it will be marked `pytest.skip` if the file is absent. The drift is a real Hermes-v0.16.0 fact, not a v0.17.0 upgrade concern.

**[major]** 05-script-1-task-e-toggle.md :: E3.build_skills_prompt row (line 17) and E6 row (line 20)

  - Concern: Two Task E site anchors in the plan do not match the actual Hermes v0.16.0 source. (a) E3 cites `agent/prompt_builder.py:1373` with anchor `return "<available_skills>\n" +`. Verified at line 1373 of the real source: the literal is `"If a skill has issues, fix it with skill_manage(action='patch').\n"` (the prose immediately preceding the available_skills block). The f-string-style `<available_skills>` concatenation lives at lines 1378-1380 (`"<available_skills>\n" + "\n".join(index_lines) + "\n</available_skills>\n"`). The plan's anchor text matches a portion of that but the line number is wrong. (b) E6 cites `tools/skill_manager_tool.py:~992` with anchor `description="""Create, edit, patch, delete, write_file, or remove_file`. The `~` is a hedge, but the SKILL_MANAGE_SCHEMA in v0.16.0 may not be at exactly 992 — the plan does not provide the test that pins the line. On a real Hermes install, the patch will TEXT_DRIFT on the first `--check` run.
  - Fix: Update E3's `current_text` to match the literal at the actual anchor line. Either re-anchor to line 1378 (`"<available_skills>\n"`) or to line 1373 with the corrected prose text. Update E6 with a pinned line number (no `~`) after reading the live source: run `grep -n 'description="""Create, edit, patch' tools/skill_manager_tool.py` and replace the `~992` with the actual line. Add the same `test_anchors_match_hermes_v0_16_0` integration test from finding 3 above.

**[major]** 07-skill-creator-migration.md :: Eval pipeline (lines 105-115) + adapter for Hermes event shape

  - Concern: Plan says the Hermes event shape is `{event: "message", role: "assistant", content: "..."}` (single canonical, from Q2 default). Verified at anthropicCreator.json (line 333) the proposed migration is `{event, role, content}` but the JSON marks the exact field map as TBD per Q2. Plan's T3.011 row claims this binding is fixed, but the underlying Hermes event shape has not been read from the live install (the plan defers to Phase 5). This is a T3-row pinning a TBD-claimed binding: the migration-fidelity test `test_T3_011` cannot pass against the live Hermes install because the live event shape is unknown at planning time. The plan should either (a) make T3.011 a parametric test that accepts the candidate event shapes from `_research/taskECandidates.json` AND the research JSON, or (b) escalate Q2 to a planning-time blocker (read `~/.hermes/hermes-agent/hermes_cli/streaming/` before file 07 lands).
  - Fix: Make `test_T3_011_event_shape_adapter` parametric: it must accept any of the candidate Hermes event shapes documented in the research (e.g., `{event, role, content}`, `{type, message, content}`, `{event_type, data}`) and normalize each into the Anthropic-shaped dict the rest of the pipeline expects. Document the candidate set in 07 §Eval pipeline. Add a "Phase 5 first step" sub-task to 11-sub-agent-delegation-map.md: the E-skill workstream must read `~/.hermes/hermes-agent/hermes_cli/streaming/` (read-only) within the first hour and either (i) confirm one of the candidates and lock T3.011, or (ii) update the adapter and T3 row. Until then, the T3.011 row is speculative.

**[major]** 07-skill-creator-migration.md :: T3.006 — `--model claude-...` replacement

  - Concern: T3.006 row in the plan's T3 inventory says `scripts/run_eval.py (CLI flags)` has `--model claude-...` and the Hermes replacement is `--model hermes-...` (or omit). Verified at the actual vendored source: `scripts/run_eval.py:268` has `parser.add_argument("--model", default=None, help="Model to use for claude -p (default: user's configured model)")` — there is no literal `claude-...` default value. The Anthropic default-model is set at the runtime level (per CLI auth), not as a literal string in the flag. Plan's T3.006 misstates the binding. The replacement `--model hermes-...` is also wrong because the Hermes CLI uses different model identifiers (e.g., `nousresearch/hermes-...`). Plan's row is invented.
  - Fix: Replace T3.006 with the actual binding at run_eval.py:268 and improve_description.py:199 (`--model` flag, with `default=None`): the help text references "claude -p", which is a Claude-specific string. The Hermes replacement is the help text only (not a model-id literal): update the help string to reference `hermes -p` and pin a Hermes-default model id (or leave it None and let the runtime pick). Add a per-row test that greps the migrated file for the help-text change.

**[major]** 06-script-2-profiles.md :: AC-3.8 (clear_skills_system_prompt_cache) — branch coverage

  - Concern: Plan 06 says Script #2 calls `clear_skills_system_prompt_cache(clear_snapshot=True)` after every successful flip, with a fallback to deleting `~/.hermes/.skills_prompt_snapshot.json` directly if the function does not exist. The TDD list has `test_apply_calls_clear_skills_system_prompt_cache` (a spy that records the call) but does NOT have a test that asserts the cache file is actually deleted. A spy test passes even if the function is a no-op in the real implementation. Migration-fidelity to profileSystem.json (which identifies the snapshot path as the load-bearing artifact) is incomplete without a file-system-level test.
  - Fix: Add `test_apply_invalidates_prompt_snapshot_file`: pre-populate `<HERMES_HOME>/.skills_prompt_snapshot.json` in the tmp_path fixture, run `--apply`, assert the file is gone (or its mtime is newer). Add a second test `test_apply_falls_back_to_direct_delete_when_function_missing`: monkeypatch the function to raise ImportError, run `--apply`, assert the cache file is still gone (the fallback path). Both tests must run against the tmp_path HERMES_HOME fixture.

**[major]** 07-skill-creator-migration.md :: test_no_claude_invocations_remain (lines 137)

  - Concern: Test description says it greps the migrated skill tree for `\bclaude\b` and asserts zero hits in code/commands, with allowed mentions only in `MIGRATION.skill-port.md` and bilingual advisories. This test will FAIL on the migrated `eval-viewer/generate_review.py` if that file is preserved unchanged — verified vs the JSON: generate_review.py is "stdlib-only" and host-agnostic, so it does NOT need migration. But `eval-viewer/viewer.html` (44998 bytes) is an HTML/JS bundle that is likely full of `claude` mentions (e.g., "Claude Code" branding in the UI). The test must enumerate ALL files that are allowed to retain `claude` mentions, not just `MIGRATION.skill-port.md` and "bilingual advisories".
  - Fix: Update `test_no_claude_invocations_remain` to enumerate an explicit allowlist of files that may contain `claude` (e.g., `eval-viewer/viewer.html` if it has branding; `MIGRATION.skill-port.md`; `SKILL.md`'s `description` if it contains "Anthropic skill-creator"). The test should grep ALL files in the migrated skill tree, collect hits, and assert every hit is on the allowlist with a `reason` annotation. Add a TDD step to enumerate the allowlist in 07 §TDD test list as a fixture file (`tests/fixtures/claude_allowlist.json`) so the allowlist is itself testable.

**[minor]** 07-skill-creator-migration.md :: scripts/utils.py (vendored) — host-agnostic confirmation

  - Concern: Plan 07 line 113 (`scripts/utils.py` — shared helpers) lists this as a preserved file. Verified at the vendored source: `scripts/utils.py:1` has the docstring `"""Shared utilities for skill-creator scripts."""` (Claude-specific) and `utils.py:7` defines `parse_skill_md`. The module docstring references "skill-creator scripts" generically (not Claude). But the parse_skill_md function is host-agnostic only if it doesn't reference any Claude-specific function. The plan does not list the parse_skill_md implementation contract. Migration-fidelity: if utils.py is preserved unchanged, the plan must verify the function's signature and behavior match what the rest of the migrated pipeline assumes.
  - Fix: Add a test `test_utils_py_preserved_unchanged` that asserts the byte-identical content of `scripts/utils.py` from the vendored source is present in the migrated tree, and add `test_parse_skill_md_signature` that asserts the function's signature is `(skill_path: Path) -> tuple[str, str, str]` (the current Anthropic contract) so the rest of the migrated pipeline can rely on it. Both are 100% hermetic (no Hermes install required).

**[minor]** 08-migration-note-format.md :: MIGRATION.skill-port.md row count assertion (line 143)

  - Concern: Plan asserts `test_migration_note_exhaustive_skill_port` checks the T3 row count == 15. Verified: the plan's 07 T3 table has 15 rows, the MIGRATION.skill-port.md template in 08 also has 15 rows. This is consistent. BUT the row labeled T3.006 (the `--model` flag row) has a questionable Hermes equivalent (see major finding 6) and the row labeled T3.010 references a non-existent Python construct. After the major fixes above, the row count may change. The exhaustiveness test should be `>= 15` (allowing for additional rows as the vendor source evolves) or a parametric count with a `HERMES_SKILL_CREATOR_T3_ROW_COUNT` env var.
  - Fix: Replace the hardcoded `== 15` with `>= 15` plus a `HERMES_SKILL_CREATOR_T3_ROW_COUNT` env var that pins the expected count in CI; local runs use the env var's default. Add a test `test_migration_note_t3_count_matches_vendored_source` that reads the vendored T3 inventory from the research JSON (`plans/_research/anthropicCreator.json::claudeSpecificInvocations`) and asserts the migration note row count == that count, NOT a hardcoded 15.

**[minor]** 07-skill-creator-migration.md :: Eval viewer (line 123) — file:// URL semantics

  - Concern: Plan says "generate_review.py --static writes both files; integration test loads the HTML in a headless browser (or a HTML parser) and asserts the JS reads `feedback.json` correctly." The vendored `eval-viewer/generate_review.py:1` docstring is "Generate and serve a review page for eval results" — and the JSON marks it as host-agnostic. The TDD test `test_eval_viewer_static_open` is a positive assertion but the plan never enumerates what "JS reads `feedback.json` correctly" means in testable terms (e.g., a specific JSON path, a specific DOM element). Migration-fidelity to the Anthropic eval-viewer UX requires a snapshot test against the generated HTML's structure.
  - Fix: Add a snapshot fixture `tests/snapshots/eval_viewer_static_open.html` containing the golden HTML output of `generate_review.py --static`; the test asserts byte-identical output. Add a smoke test that parses the HTML, locates the `<script>` tag that fetches `feedback.json`, and asserts the fetch URL is a relative path (not an absolute localhost URL). This is a 100% hermetic test; no Hermes install required.

**[minor]** 06-script-2-profiles.md :: Directory walk correctness test list (lines 167-170)

  - Concern: Plan enumerates `test_walks_profile_dirs_set` asserting the audit walks `{memories, sessions, skills, skins, logs, plans, workspace, cron, home}`. The list is verified vs profileSystem.json (which cites hermes_cli/profiles.py:39-53 `_PROFILE_DIRS`). The test does NOT include a test for what happens if `_PROFILE_DIRS` is changed in a future Hermes version (e.g., a new subdir is added). Migration-fidelity to the research JSON's claim "list-of-truth is _PROFILE_DIRS" requires the test to read `_PROFILE_DIRS` from the live source at test time, not hardcode the set. Otherwise the test is brittle to upstream changes.
  - Fix: Refactor `test_walks_profile_dirs_set` to read `_PROFILE_DIRS` from `hermes_cli.profiles._PROFILE_DIRS` at test time (read-only) and assert the audit walks exactly that set. Add `pytest.skip` if the Hermes source is absent. This is the same no-touch-sentinel pattern as `real_hermes_agent_sentinel`.

**[minor]** 12-risks-and-open-questions.md :: R5 (line 93) — vendor-bundled vs user-local precedence

  - Concern: Plan documents the precedence rule "bundled in register_skill, user-local at ~/.hermes/skills/ for skill_view" but does not enumerate a test for the dual-source case. If a user has their own skill-creator with a different description, the system-prompt index will show the bundled one while `skill_view(name='skill-creator')` returns the user-local one. This is a documented UX surprise; migration-fidelity requires the plan to expose the precedence rule via a TDD test.
  - Fix: Add `test_register_skill_uses_bundled_when_user_local_present` to 03 §TDD test list. Fixture: pre-populate `<HERMES_HOME>/skills/<cat>/skill-creator/SKILL.md` with a known different description; assert the plugin's `register_skill` call uses the BUNDLED frontmatter (asserts via mock on `ctx.register_skill`). Add a parallel test in 07 §TDD: `test_skill_view_resolves_user_local_first` (Hermes's loader precedence; verified against hermesSkillConventions.json::skillDirectoryLayout which says "bare name = local skill"). The two tests together pin the precedence contract.

**[nit]** 07-skill-creator-migration.md :: T3.007 (SKILL.md:432) — Claude.ai reference

  - Concern: T3.007 row in the T3 inventory says the replacement is `nousresearch.com/hermes` (or remove the URL). Verified: the line 432 of the actual vendored SKILL.md does NOT contain a URL — it contains the prose "This section requires the `claude` CLI tool (specifically `claude -p`) which is only available in Claude Code. Skip it if you're on Claude.ai." The "claude.ai" is a proper noun, not a URL. The proposed replacement `nousresearch.com/hermes` is therefore a fabrication.
  - Fix: Update T3.007's proposed replacement to prose-only: replace "Claude.ai" with "Hermes headless mode" (no URL). Delete the `nousresearch.com/hermes` reference. Update the T3.007 test to grep for the new prose string.

**[nit]** 12-risks-and-open-questions.md :: Escalation log dates (line 101)

  - Concern: Escalation log table uses `2026-06-17` for all 9 entries (E1–E9). Today is 2026-06-17 (per the date context). This is consistent but the entries are dated the same day they were decided, with no follow-up dates. A future reviewer comparing the plan to a future Hermes release will have no audit trail of when each escalation was acted on. Migration-fidelity to a research-citation discipline requires per-decision audit dates.
  - Fix: No code change required. Add a column to the escalation log: "Actioned at" with the date each E1–E9 entry was actually implemented (e.g., the date the helper file `_subprocess.py` was created, the date the static-AST advisory was added). For now, fill with "2026-06-17 (planned for Phase 5 / workstream B-D)". This is a documentation nit; no AC impact.

**[nit]** 03-plugin-spec.md :: _advisory.py (line 76) — PATCHED_CAP_REFERENCE constant

  - Concern: Plan defines `PATCHED_CAP_REFERENCE = "MAX_DESCRIPTION_LENGTH"` as the constant the patched function uses. Verified at the live Hermes source: `tools/skills_tool.py:95` has `MAX_DESCRIPTION_LENGTH = 1024` and `tools/skill_manager_tool.py:112` has the same. The 04-script-1-patch.md plan replaces `if len(desc) > 60:` with `if len(desc) > MAX_DESCRIPTION_LENGTH:` and adds the import `from tools.skills_tool import MAX_DESCRIPTION_LENGTH`. This is consistent. BUT: the 03 plan's `_advisory.py` static-AST check looks for the literal `MAX_DESCRIPTION_LENGTH` Name node. If a future Hermes version uses a different constant (e.g., `DESCRIPTION_MAX_LENGTH`), the advisory will mis-report "unpatched" forever. Migration-fidelity: the static-AST detection should match either the constant OR a configurable threshold.
  - Fix: Generalize `_advisory.py::detect_cap_state` to also accept a numeric comparator `> 1024` as evidence of the patched state. The current implementation only matches the Name `MAX_DESCRIPTION_LENGTH`; a future patch that inlines the constant 1024 will be mis-detected. Add `if isinstance(comparator, ast.Constant) and isinstance(comparator.value, int) and comparator.value > 60: return "patched"` to the AST walk. This is a 2-line addition that future-proofs the advisory.

---

