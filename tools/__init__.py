"""tools — project-level helper scripts and pre-commit hook implementations.

Each module in this package is invoked either directly (e.g.
``python3 tools/check_line_count.py``) or via the project's
``pre-commit`` configuration.
"""
