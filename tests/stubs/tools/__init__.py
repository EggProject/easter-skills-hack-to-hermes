"""tools stub package — see tests/stubs/__init__.py for context.

NOTE: This shadows the project's own ``tools`` package from mypy's
perspective when ``MYPYPATH`` includes this directory. The real
``tools/`` is still on the runtime path; mypy just consults these
stubs for type resolution. Do NOT add real logic here.
"""
